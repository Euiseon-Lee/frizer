# 다음 작업 인계 · 2026-09-13

## 현재 상태

- 등록·수정 검증은 novalidate로 브라우저 팝업을 끄고 항목별 메시지·오류 테두리·첫 오류 포커스로 통일했다. 상단 중복 오류 요약은 제거했으며 수정 충돌 등 전역 오류는 저장 버튼 근처에 표시한다. 필수값과 날짜·위치 오류를 함께 수집하고 입력값을 보존한다. 문구는 ‘입력해줘.’로 통일했다.
- 모든 페이지의 브라우저 제목은 FR!ZER. 홈·목록·등록·기록 상단의 텍스트 시작점과 제목/설명 크기를 통일했다(기본 제목 28px, 360px 이하 25px, 설명 14px). 모든 탭 상단 영역은 홈 기준 252px 높이로 통일하고 이미지 크기·위치도 같은 규칙을 사용한다. 목록 제목은 우리 집에 뭐 있지?로 변경했다.
- 검증 통일 전체 테스트 151개 통과 후 상단 요약 제거·문구 변경에 대해 관련 통합 테스트 54개 및 bootJar 통과.

- 음식 수정 구현 완료: 상세의 수정 버튼 → 등록 폼 재사용 → 상세 복귀. 모든 입력값을 불러오며, 기존 자유 문자열 수량은 숫자·단위를 직접 확인받는다.
- 실제 변경이 없으면 UPDATE·수정 시각 갱신 없음. 변경 시 전체 변경 전후 내용을 food_history.changes_text에 저장하고 기록 화면에서 표시한다. V6는 UPDATE 이벤트와 변경 내역 컬럼을 추가한다.
- 보관 중 음식만 수정 가능. 행 잠금과 expectedUpdatedAt 확인으로 오래된 화면의 덮어쓰기를 차단하며 음식·이력 저장은 한 트랜잭션이다.
- 최초 등록 후 수정이 없으면 상세에 등록 시각만 표시한다. 위치 미선택 문구는 ‘아앗 필수 정보라구!’, 02의 ‘보관할 장소를 골라줘.’ 안내는 제거했다.
- 최신 test + bootJar: 148개 통과. 로컬 V6 적용 및 앱 재시작 완료. 기존 음식·이력 내용 해시와 건수 일치. 커밋·push하지 않음.

- 프로젝트: FR!ZER, 원래 작업 경로 `C:\dev\frizer`. Java 패키지 `com.euiseon.friger`.
- 등록·목록·위치 필터·상세·홈 요약·기록 조회 구현 완료. 기준 커밋 `2e55cc1` 이후 등록 안내와 입력 UI를 다듬었다.
- 최신 문구는 실제 템플릿이 기준이다. docs/ui-v5는 원래 패키지 명세이며 사용자 후속 문구 변경이 우선한다.
- 하단 탭: 홈 / 뭐 있지? / 등록 / 기록.
- 목록 상단: 쪼코랑 같이 찾아보자! / 우리 집에 뭐가 있더라? / 한 줄로 정렬시켰어.
- 빈 위치: 여기에는 음식이 없어. 권유 문장 제거, 등록 버튼과 제목 사이 16px 간격.
- 등록 화면은 친근한 반말로 통일. messages.properties의 foodForm 전용 키를 사용하고 공통 오류 메시지·DTO·저장 규칙은 유지했다.
- 배달 잔반은 위치 미선택 제출 시 냉동실 저장. 화면에서 위치가 선택된 것으로 표현하지 않는다.
- 배달 잔반 선택 시 냉동 유형은 직접 냉동으로 자동 선택되며 시판 냉동식품 선택지는 제거된다. 다른 출처로 전환하면 선택지가 복원된다. 서버는 기존부터 직접 냉동으로 정규화한다.
- 오늘 넣었어 체크 시 서울 기준 오늘 날짜 입력. 날짜 직접 수정 시 체크 해제. 빈 냉동일은 미상이다.
- 새 등록 수량은 숫자(quantity_amount, NUMERIC(12,3))와 자유 입력 단위(quantity_unit, VARCHAR(10))를 필수로 받는다. 수량은 0 초과, 정수 9자리·소수 2자리 이내이며 단위는 앞뒤 공백 제거 후 10자 이내다. quantity_text는 숫자와 단위를 합친 표시 및 이력용 문자열로 유지한다. V5는 기존 재고·이력을 변경하지 않고 새 컬럼만 NULL로 추가한다. 기존 수량은 자동 변환하지 않는다. 용량은 선택 자유 문자열이다. 유통기한과 소비기한은 각각 선택 입력이며 서로 대체하지 않는다.

## 이미지 확인 결과

- 빈 목록은 puppy-tilt / puppy-sit / empty-curious 무작위 후보를 사용한다.
- 첨부 사진의 empty-curious는 V5 C04, 실제 URL `/assets/choco/empty-curious.png`.
- src/main/resources/static 및 build/resources/main 파일과 HTTP 응답 모두 V5 가공본 SHA-256 `07ba7417f946ae6f4314623fe8af5481b001ea678c67c06c9d18b0e69cce8d5a`와 일치.
- V5 원본 SHA-256은 `24e1ec6c5045f8bf7729fdbe4855910a69cc9895d7b301f6db8dbd9f0687e042`. 원본과 가공본 모두 1751×2048 RGBA, 하단에 몸통 픽셀이 남는 상반신 구도.
- CSS contain, 120×130px, 부모 overflow visible로 추가 잘림 없음. 누끼 재가공·신체 생성·이미지 교체는 하지 않았다.
- V5 A 사진 16장 유지. 이미지 원본 패키지는 이전 작업 경로의 build/ui-reference-v5에 있으므로 새 worktree에는 없을 수 있다.

## 검증 및 실행

- 수량·단위 분리 적용 후 test + bootJar: 139개 통과. V5 로컬 적용 및 앱 재시작 완료. 기존 음식·이력은 적용 전후 건수와 전체 내용 해시가 일치한다.
- 사용자의 지시에 따라 이후 작업은 C:\dev\frizer 원본 경로에서만 진행한다. 별도 worktree는 사용하지 않는다.

- 기준 기능 커밋에서 test + bootJar: 123개 통과.
- 등록 문구 변경 후 InventoryIntegrationTest 26개 통과. 이후 배달 잔반 선택지 고정·복원, 상단/빈 목록 문구·여백은 브라우저와 HTTP로 확인했다.
- 로컬 앱은 localhost:8080, PostgreSQL Docker frizer-postgres-1은 55432 사용. 5432의 별도 PostgreSQL은 건드리지 않는다.
- JDK: `C:\dev\frizer\.gradle\jdks\jdk21.0.12_9`. 비밀정보는 Git에서 제외한 .env에 있다. 새 worktree에는 .env나 JDK가 복사되지 않을 수 있다.
- 사용자 재고 데이터는 보존한다. 커밋·push는 사용자의 명시 요청이 있을 때 진행한다. 이번에는 로컬 커밋만 요청받았다.

## 다음 논의

새 기능 구현 전에 사용자의 다음 지시를 받는다. 추천 순서:

1. 음식 수정은 구현 완료. 사용자 확인 후 소비·폐기 정책 논의.
2. 소비 / 폐기 처리와 이력.
3. 보관 위치 이동과 이력.
4. 웹 다중 선택 물리 삭제.
5. 엑셀 다운로드·업로드 대조 처리.

새 등록의 숫자·단위 분리 입력을 구현했다. 다음 소비·폐기 구현에서는 같은 단위 안에서의 차감을 기본으로 논의한다. 단위 자동 환산은 하지 않는다. 기존 자유 문자열 재고는 숫자·단위 확인 후 전환하는 별도 흐름이 필요하며, 이를 임의로 변환하거나 차감하지 않는다. 소비·폐기 기능 자체는 아직 미구현이다.

엑셀 작업은 추가 / 수정 / 소비 / 폐기로 합의했다. 물리 삭제는 웹에서 별도 처리하며 음식과 연결 이력을 함께 삭제하는 것으로 사용자가 정정했다. 엑셀 양식·충돌·재업로드 정책은 미확정이다. docs/BULK_REGISTRATION_DRAFT.md와 docs/DATE_POLICY_DRAFT.md를 참고한다.

PWA·인증·배포·추천 점수는 미구현이며 이번 후속 작업에 자동으로 포함하지 않는다.
