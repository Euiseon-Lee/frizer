# FR!ZER STEP 3 — 앱의 기본 개념과 재고 관리 흐름

최종 정리: 2026-09-16. 이 문서 앞부분은 현재 구현과 사용자 확정 정책의 개요다. 기능별 저장 계약은 연결된 기준 문서에서 관리한다.

**STEP 3은 진행 중이다.** 기본 재고 관리는 구현했으나 수량 분리·오등록 삭제 등 전체 설계의 미구현 범위가 남아 있다. 아직 STEP 3 완료 또는 STEP 4 시작으로 표시하지 않는다. 다음 단계의 범위와 명칭은 별도로 확정한다.

## 1. 용어 정의

| 용어 | 앱에서의 의미 | 예시 |
| --- | --- | --- |
| 음식 / 음식 그룹 (MASTER) | 사용자가 같은 음식으로 관리하는 묶음. 현재 음식명·분류를 공유한다. | 풀무원 두부 |
| 개별 항목 (ITEM) | 현재 잔량·단위·보관 정보·이력을 독립적으로 관리하는 재고 한 묶음 | 월요일에 등록한 두부 2모 |
| 구매분 | 실제 구매로 들어온 개별 항목. 모든 항목이 구매분인 것은 아니다. | 금요일에 산 두부 3모 |
| 신규 등록 | 새로운 음식 그룹과 첫 개별 항목을 함께 생성 | 처음 관리하는 연두부 등록 |
| 추가 등록 | 선택한 음식 그룹 아래 새 개별 항목을 생성. 기존 항목의 잔량을 더하지 않는다. | 기존 두부 아래 오늘 들어온 3모 추가 |
| 현재 잔량 | 해당 항목에 지금 남아 있다고 기록된 수량 | 1.5모 |
| 최초 등록 수량 | 해당 항목의 등록 이력에 저장된 당시 수량. 실제 구매량과 항상 같지는 않다. | 처음 등록할 때 남아 있던 2모 |
| 잔량 정정 | 수정 화면에서 현재 잔량을 바로잡는 작업. 증가해도 신규 구매가 아니다. | 화면 1.5모 → 실물 확인 후 1모 |
| 소비 / 폐기 | 사유를 기록하며 잔량을 차감하는 작업 | 먹었어 0.5모 / 버렸어 0.5모 |
| 보관 종료 | 소비·폐기로 잔량이 0이 된 개별 항목의 상태 | 항목과 이력은 남아 있음 |
| 처리 취소 | 조건이 허용하는 마지막 소비·폐기를 되돌리는 작업 | 소비한 0.5모 복구 |
| 음식 이동 | 항목의 소속 음식 그룹을 바꾸는 작업. 보관 장소 변경과 다르다. | 잘못 연결한 항목을 두부 그룹으로 이동 |
| 보관 위치 변경 | 같은 항목의 실온·냉장실·냉동실 정보를 변경 | 항목 전체를 냉동실로 옮김 |
| 수량 분리 — 미구현 | 한 항목 일부를 별도 항목으로 나누고 원본 관계를 보존하는 작업 | 2모 중 1모를 따로 보관 |
| 없어졌어 — 미구현 | 기록상 잔량이 있으나 실물이 없는 경우를 소비·폐기와 구분하는 후속 처리 | 분실·사유 불명 재고 부족 |

화면의 ‘개별 구매 목록/항목’은 현재 사용 중인 UI 명칭이다. 내부 ITEM에는 직접 조리·배달 잔반·받은 음식도 포함되므로, 구매 여부를 화면 명칭이나 생성 이벤트만으로 판정하지 않는다. 이 문서 정리가 모든 화면 명칭을 바꾸는 것은 아니다.

## 2. 기본 구조와 공통 원칙

### 사용자 소유권과 등급 — V15 로컬 구현

업무 데이터의 소유자는 `app_user.user_id`다. 로그인명 변경이나 권한 변경으로 소유권이 변하지 않는다. 음식·구매·이력과 모든 요청 영수증/엑셀 미리보기에 사용자 FK가 있고, 기존 데이터는 단일 본인 계정으로 보존한다. 전환 절차와 현재 배포 상태는 [실행 안내](README.md)의 사용자 계정 전환 절을 따른다.

지금의 목표는 운영 본인 USER 계정과 테스트 USER 계정의 독립 사용이다. 닉네임·사진·공개 회원가입·관리자 화면은 이번 범위가 아니다. 등급 값 `ADMIN`과 `USER`만 미리 마련한다.

**후속 관리자 정책:** 관리자는 본인뿐 아니라 모든 사용자의 음식 데이터에 접근할 수 있어야 한다. 일반 사용자의 소유권 조건을 클라이언트 파라미터로 해제하지 않는다. 서버의 별도 관리자 접근 정책에서 실제 행위자 ID, 대상 소유자 ID, 권한을 검증하고 조회/수정 범위를 전달한다. 관리자 작업에도 음식의 기존 소유자를 유지하고 행위자와 대상 소유자를 구분해 기록한다. 현재 `CurrentUser`는 본인 소유만 반환하며 이 관리자 접근은 아직 구현하지 않았다.

MyBatis `OwnerLanguageDriver`는 명시적 `#{_userId}` 바인딩을 인증 principal로 채운다. SQL 문자열 치환이나 클라이언트 입력을 사용하지 않고, 캐시 키 생성 전에 소유자를 포함하여 사용자 간 캐시 재사용도 차단한다. JdbcTemplate을 사용하는 엑셀 경로에도 같은 소유자 조건을 직접 적용한다.

음식·항목·이력 간 소유권은 복합 FK로 검증한다. 영수증에는 `user_id` FK와 사용자별 요청 PK를 적용하되 과거 대상 ID의 생존을 요구하지 않는다. 항목 잠금 전에 생성하는 요청 영수증에 새 항목 FK를 붙이면 KEY SHARE 잠금이 기존 잠금 순서를 뒤집어 동시 수량 처리에서 교착을 만들 수 있기 때문이다. 서비스의 소유자 범위 확인과 트랜잭션 계약을 유지한다.

```text
음식 그룹: 두부
 ├─ 항목 A: 9월 16일 등록 / 냉장실 / 현재 1모
 │   └─ 등록 → 소비 → 잔량 정정 등의 이력
 └─ 항목 B: 9월 18일 등록 / 냉장실 / 현재 3모
     └─ 등록 이력
```

- 하나의 음식 그룹에 여러 항목이 속하고, 각 항목에 여러 이력이 연결된다. 수량·소비·폐기·취소는 항목 단위로 처리한다.
- 음식의 구분은 사용자가 정한다. 두부·순두부, 서로 다른 브랜드는 의도에 따라 별개로 등록할 수 있다. 같은 이름도 허용하며 자동으로 합치지 않는다.
- 재구매·추가 유입은 새 항목을 만든다. 수정은 기존 항목의 잔량·정보 정정이며 최초 등록 수량을 바꾸지 않는다.
- 출처는 장보기·배달 잔반·직접 조리·부모님·기타 등을 지원한다. 초기 등록은 현재 남아 있는 재고를 입력할 수 있다. 초기 보유 재고를 구별하는 별도 기능은 아직 없으며 입력 수량을 무조건 구매량으로 해석하지 않는다.
- 등록·수정·소비·폐기 입력은 정수 최대 9자리, 소수 최대 2자리로 통일한다. 현재 DB의 NUMERIC(12,3) 저장 타입과 과거 이력은 유지한다.
- 새 등록·수량 추가·잔량 수정·처리할 수량은 0보다 커야 한다. 처리할 수량은 현재 잔량을 넘을 수 없다. 수정에서 0으로 종료하는 기능은 없다.
- 단위 자동 환산은 하지 않는다. 단위가 바뀐 수정은 숫자 차이를 계산하지 않고 전후 값을 기록한다.
- 구조화된 현재 재고는 보관 중(ACTIVE, 잔량 양수) 또는 종료(DEPLETED, 잔량 0)다. 과거 비구조화 항목은 수량 작업 전에 사용자가 숫자·단위를 확인한다.
- 재고가 소진되어도 음식 그룹·항목·이력을 보존한다. 음식 이동 후 원본 그룹 정리는 별도 규칙이며 종료 항목이 남은 그룹은 삭제하지 않는다.
- 현재 코드는 V15까지 구현돼 있고 운영 DB는 아직 V14다. 실제 컬럼·제약·이벤트 종류는 migration과 코드가 기준이다. 아래 초기 설계에 나온 모든 항목이 구현된 것은 아니다.

## 3. 작업별 흐름

### 등록과 수량 추가

1. 기존 음식을 검색하고 사용자가 연결 대상을 결정한다.
2. 새 음식이면 음식 그룹과 첫 항목을 등록한다.
3. 기존 음식이면 ‘추가 등록’로 새 항목을 등록한다.
4. 항목별 등록 당시 수량·단위·출처 등의 이력을 남긴다.

등록 경로와 구매 여부는 별개다. 처음 사는 음식도 신규 등록으로 구매가 기록될 수 있다. 이미 있는 음식을 다시 샀다면 수량 추가로 별도 항목을 만들고, 기존 항목의 잔량 수정으로 대체하지 않는다. 향후 구매 통계는 출처 등 저장된 사실을 함께 판단해야 한다.

### 수정과 복귀

- 수정의 수량 증감은 항상 현재 잔량 정정이다. 증가분을 구매로 취급하지 않는다.
- 음식명·분류는 소속 음식 그룹에서 공유한다. 수량·보관 정보·메모 등은 해당 항목의 정보다.
- 변경된 정보는 수정 이력으로 남기며, 변경 없이 저장하면 추가 이력을 남기지 않는다.
- 수정 완료 → 소속 음식의 개별 구매 목록. 수정 취소 → 해당 항목 상세.
- 진입 시 보관 위치·경고·종료 조건은 오류 재표시·취소·완료까지 유지한다. 복귀용 보관 위치 필터와 실제 항목의 보관 위치는 별도 값이다.
- 수량 수정 안내: 잔량이 수정되니, 새로 구매했다면 ‘추가 등록’을 이용해줘.

### 소비·폐기와 종료

```text
보관 중
 ├─ 일부 먹었어 / 버렸어 → 잔량 차감 → 보관 중 유지
 └─ 전부 먹었어 / 버렸어 → 잔량 0 → 종료 목록

종료
 ├─ 가능한 마지막 처리 취소 → 처리량 복구 → 보관 중
 └─ 재구매 / 추가 유입 → 별도 새 항목 생성, 종료 항목은 보존
```

- 소비·폐기 확인 화면에서 처리 수량을 직접 입력한다. 기본값은 현재 잔량 전체다.
- 처리 완료는 기존 조건의 구매 목록으로 복귀한다. 완료 알림은 영역 또는 × 버튼으로 닫고, 알림 안의 처리 취소 링크도 함께 숨겨진다. 알림을 닫아도 항목 상세의 취소 가능 여부는 바뀌지 않는다.
- 음식 목록의 전체·위치 탭은 보관 중 항목을, 종료 탭은 종료 항목을 표시한다. 혼합 그룹은 양쪽에서 볼 수 있다.
- 종료 목록에서는 경고 필터를 적용하지 않는다. 복귀를 위한 선택값은 보존한다.
- 종료 화면은 최초 등록 수량과 실제 종료 일시를 표시한다. 수정 일시를 종료 일시로 대신하지 않는다.
- ‘없어졌어’는 후속 기능이다. 현재 소비·폐기 또는 삭제로 자동 변환하지 않는다.

### 처리 취소와 이력

- 가장 최근 소비·폐기만 후보이며 이미 취소된 처리는 다시 취소할 수 없다. 과거 처리까지 연쇄 취소하지 않는다.
- 이후 수량·단위·소속·보관 위치·냉동 유형·개봉일·냉동 시작일이 바뀌면 차단한다. 값을 원래대로 돌려도 변경 이력은 남는다.
- 이름·분류·메모·유통/소비기한 정정은 유지하며, 취소할 때 스냅샷 전체를 덮어쓰지 않는다.
- 취소는 처리한 수량만 복구하고 별도 취소 이력을 추가한다. 원래 소비·폐기 이력은 삭제하지 않는다.
- 수량 변경 이력은 등록·수량/단위 수정·소비·폐기·취소를 최신순으로 보여준다. 숫자·단위가 그대로인 수정은 제외한다.
- 등록 수량은 당시 기록에서 가져온다. 구조화 값이 없는 과거 등록은 원문을 표시하고 현재 잔량에서 추정하지 않는다.

### 음식 이동과 수량 분리

- 구현된 개별 이동: 항목 전체의 소속을 기존 음식 또는 새 음식으로 변경한다. 항목 ID와 수량·이력은 보존한다.
- 구현된 음식 전체 이동: 원본 음식의 항목들을 대상 음식으로 옮긴다. 항목 수량을 하나로 합치지 않는다.
- 보관 위치 변경: 항목 전체의 위치를 수정한다. 일부만 다른 조건으로 보관하려면 수량 분리가 필요하다.
- 수량 분리는 아직 미구현이다. 원본 차감과 새 항목 생성은 연결된 한 작업으로 기록하고, 분리 자체를 소비·신규 구매로 계산하지 않는 원칙은 확정됐다.
- 예: 같은 단위의 2팩을 나누면 기존 1팩 + 새 1팩이 된다. 조리 후 팩→g/인분처럼 단위가 달라지는 경우의 입력·연결·검증 방식은 아직 확정하지 않았다.
- 사용자는 조리 후 남은 음식을 반복 보관하는 흐름을 자주 사용한다. 분리 이후 소비·재보관 이력, 냉동 유형·날짜 처리까지 다음 설계에서 정한다.

## 4. 구현 상태와 운영 준비

| 범위 | 현재 상태 | 진행 시점 |
| --- | --- | --- |
| 음식 그룹·개별 항목, 등록·수량 추가 | 구현 | 현재 사용 가능 |
| 수정·잔량 정정·필터 유지 복귀 | 구현 | 현재 사용 가능 |
| 소비·폐기·제한적 취소·종료 | 구현 | 현재 사용 가능 |
| 등록을 포함한 수량 변경 이력 | 구현 | 현재 사용 가능 |
| 음식 전체 이동·보관 중 항목 개별 이동 | 구현 | 현재 사용 가능 |
| 엑셀 일괄 등록 | 다운로드·미리보기·전체 반영·중복 방지 구현 | 현재 사용 가능, 초기 목록 준비 후 입력 |
| 수량 분리·조리 후 재보관 | 미구현, 세부 흐름 미확정 | 실제 사용에 중요하므로 우선 설계, 출시 범위 확정 필요 |
| 오등록 물리 삭제 | 미구현, 항목과 관련 이력 삭제 원칙 | 운영 전 포함 여부 결정 필요 |
| 없어졌어 | 미구현, 소비·폐기와 구분하는 방향 | 후속 |
| 종료 항목 개별 이동·통계 | 미구현 | 운영 이후 |
| 검증 문구 유형 분류·표시 기준 | 일괄 등록 행의 누락·입력·조건·자동 안내 분류 구현 | 같은 원인 중복 안내 제거, 파일·중복 확인은 기존 별도 영역 유지 |
| 목록 검색·정렬 확장 | 미구현 | 사용 빈도에 따라 결정 |
| 배포·접근 제어·백업/복구·실기기 점검 | 운영 준비 범위 | 운영 환경과 출시 전 점검 범위 확정 필요 |

현재 기능에서 최초 등록량을 수정으로 다시 쓰지 않는 정책은 확정됐다. 별도의 최초 구매량 정정 기능을 출시 필수로 두지 않는다. 통계 기능은 운영 이후이며, 구체적인 집계·초기 재고 구분은 별도로 확정한다.

일괄 등록의 첫 출시는 새 음식/첫 항목 등록 및 기존 음식 아래 새 항목 추가다. 미리보기·행별 오류·반영 직전 재검증·중복 업로드 방지를 제공하고, 한 행이라도 오류가 있으면 전체 미반영한다. 기존 항목 수정·소비·폐기는 첫 출시 범위에서 제외한다. 양식은 `.xlsx` 최대 2MB·500개 항목이며, 세션별 미리보기 요청과 정규화한 내용 해시로 재제출·중복 업로드를 구분한다. 실제 반복 유입은 미리보기에서 명시적으로 확인한다. 세부 규칙은 일괄 등록 문서를 따른다.

## 5. 문서 역할과 확인 순서

1. 이 문서: 용어·구조·사용 흐름·현재/예정 범위.
2. [수량 설계](ITEM_QUANTITY_DESIGN.md): 수량·취소·종료의 저장 계약.
3. [이동 설계](ITEM_MOVE_DESIGN.md): 음식 간 항목 이동 계약.
4. [UI 규격](UI_DESIGN.md): 버튼명·태그·색상·화면 표현.
5. [일괄 등록 초안](BULK_REGISTRATION_DRAFT.md): 구현된 양식·업로드·중복 처리 규칙.
6. [개발 안내](README.md), [테스트 범위](STEP3_TEST_MATRIX.md): 실행·검증 방법과 검증 한계.

최근 검증: 전체 Java 473개·JavaScript 41개와 bootJar 통과(2026-09-16). 자동 냉동 보정 안내 분기와 원본 양식 보존을 포함한다. 사용자가 8080에서 최종 UI를 확인했다. 실제 모바일 기기 점검과 운영 환경 선택은 남아 있다.

아래는 초기 상세 설계 보존 구역이다. 현재 사용 흐름을 이해할 때는 위 내용부터 읽는다.

## 참고: 초기 상세 설계와 후속 구현 초안

이하 내용은 V1~V6 시점에 작성한 상세 설계다. 현재 구현 설명이 아니다.
현재 동작과 구현 여부는 이 문서 앞부분 및 기능별 기준 문서를 우선한다. stock_batch_id·parent_item_id와 ADD_STOCK·SPLIT 등의 제안은 아직 구현된 컬럼·이벤트를 뜻하지 않는다. 조리 후 분리의 단위·음식 소속 정책은 재검토 대상이다.

사용자 후속 지침: 현재 데이터는 테스트용이므로 완벽한 과거 호환에 과도하게 투자하지 않는다.
새 구조의 명확성을 우선하고, 구버전 동시 운영·임시 호환 컬럼·복잡한 운영 복구 계획은 첫 구현에서 제외한다.
테스트 데이터 초기화는 가능한 전환 선택지이며 이 문서 작성 시 실제 데이터 삭제는 수행하지 않는다.

### 1. 사용자 확정 정책

1. quantity_amount는 현재 잔량이다.
2. 최초 수량 컬럼을 별도로 두지 않는다. 신규 유입 이력에 최초 숫자·단위를 보존한다.
3. 기존 비구조화 재고는 수량 작업 전에 사용자 확인으로 구조화한다. 자동 파싱하지 않는다.
4. 단위는 trim하고 합의한 표준 단위만 대표 표기로 정규화한다. 자동 환산하지 않는다.
5. ITEM 상태는 ACTIVE / DEPLETED다.
6. 부분 분리는 기존 ITEM 차감 + 새 ITEM 하나 생성이다. 전량 변경은 분리하지 않는다.
7. stock_batch_id, parent_item_id, operation_id를 도입한다.
8. 기존 changes_text는 유지하고 신규 구조화 수량·JSONB snapshot을 병행한다.
9. 소비·폐기는 한 번에 ITEM 하나를 대상으로 한다.
10. 마지막 유효 소비·폐기만 제한적으로 취소한다.
11. 물리 삭제는 웹의 명시적 오등록 정정 기능이다. ITEM과 관련 History를 함께 삭제한다.
12. 엑셀 첫 출시는 신규 등록·기존 음식 추가 구매만 지원한다.

엑셀은 행별 오류 표시·미리보기·반영 직전 재검증·중복 업로드 방지를 제공한다.
한 행이라도 오류가 있으면 전체 미반영하며, 반영 중 실패해도 전체 트랜잭션을 취소한다.
동일 MASTER에 여러 구매분을 추가하는 행은 허용한다. 반복 MASTER ID와 중복 업로드 행은 구분한다.

음식명 중복은 허용한다. MASTER 자동 통합은 하지 않는다. 사용자가 통합해도 ITEM·구매분·이력은 보존한다.

#### 목록·상세 UI 확정 정책 (2026-09-14)

- 목록은 음식(MASTER)별로 묶어 표시한다.
- 음식 카드를 선택하면 상세에서 구매분과 개봉·보관 상태별 재고(ITEM)를 구분해 보여준다.
- 소비·폐기 등 재고 작업은 상세에서 대상 ITEM을 선택해 처리한다.
- 예: 두부 상세에 서로 다른 구매분의 미개봉 1모와 개봉 0.5모를 구분해 표시한다.
- 이 결정은 화면 구조에 대한 확정이며 STEP 3 구현 완료를 의미하지 않는다.
- 보관 위치 필터에서의 표시 범위, 서로 다른 단위의 수량 요약 등 세부 표시는 후속 논의한다.

#### 음식 병합 확정 정책 (2026-09-14)

- 사용자가 원본과 기준 음식을 선택한다. 이름을 먼저 수정할 필요가 없다.
- 예: `듀부`를 `두부`에 합치면 모든 구매분이 `두부`에 속한다. 구매분 자체는 합치거나 환산하지 않는다.
- 기준 음식의 이름과 기본 설정을 유지하며 원본 음식 항목은 제거한다.
- 연결된 재고와 이력은 보존한다. 과거에 기록된 음식명은 병합으로 덮어쓰지 않는다.
- 실행 전 이동할 재고와 보존할 이력 건수를 표시하고 확인받는다.
- 이번 병합에는 되돌리기 기능을 제공하지 않으며 확인 화면에 안내한다.

### 2. V6 기준 구현과 전체 설계의 차이

- food_item.food_id와 food_history.food_id가 현재 재고·이력 연결 키다. 이 ID를 보존한다.
- 현재 food_item에 음식명·분류와 실제 재고 정보가 함께 있다.
- quantity_amount는 NUMERIC(12,3), quantity_unit은 VARCHAR(10), quantity_text는 VARCHAR(50)이다.
- 신규 입력은 정수 9자리·소수 2자리이며 DB 수량 제약은 현재 0 초과다.
- 기존 비구조화 재고는 amount/unit 모두 NULL이며 quantity_text를 보존한다.
- 현재 상태는 ACTIVE / CONSUMED / DISCARDED다. 소비·폐기 기능 자체는 아직 미구현이다.
- 등록 이력은 changes_text TEXT에 snapshot-v1: 접두어와 JSON을 저장한다.
- 수정 이력은 changes_text에 변경 항목과 전후 값을 문자열로 저장한다.
- 현재 수정 충돌은 행 잠금과 expectedUpdatedAt 비교로 방지한다.
- MASTER, 구매분 식별, 분리, 취소, JSONB 컬럼은 아직 없다.

### 3. food_master 신규 테이블

| 컬럼 | 타입 / NULL | 의미 |
|---|---|---|
| master_id | BIGINT identity PK | 음식 식별자 |
| food_name | VARCHAR(100) NOT NULL | 대표 음식명, 공백만 입력 금지, UNIQUE 없음 |
| category | VARCHAR(50) NULL | 음식 분류 |
| default_quantity_unit | VARCHAR(10) NULL | 신규 ITEM 입력 기본값. 기존 ITEM 단위 변경 없음 |
| default_exclude_expiry_warning | BOOLEAN NOT NULL DEFAULT FALSE | 신규 ITEM 기한 경고 제외 기본값 |
| default_exclude_opened_warning | BOOLEAN NOT NULL DEFAULT FALSE | 신규 ITEM 개봉 경고 제외 기본값 |
| version_no | BIGINT NOT NULL DEFAULT 0 | 수정·통합·삭제 및 업로드 확인용 버전 |
| created_at | TIMESTAMPTZ NOT NULL | 생성 시각 |
| updated_at | TIMESTAMPTZ NOT NULL | 변경 시각 |

재고가 없어도 MASTER를 자동 삭제하지 않는다. 목록 노출과 추가 등록 검색은 별도로 처리한다.
MASTER 기본 설정은 신규 ITEM에 복사한다. 기존 ITEM 일괄 변경은 별도 명시적 작업이다.
대표 단위의 정확한 매핑표는 구현 전에 정하며, 자유 입력 영문 전체를 소문자로 바꾸지 않는다.

### 4. food_item 유지·확장

물리 PK 이름은 food_id를 유지한다. 설계에서 ITEM ID라 부르는 값이 이 컬럼이다.

| 컬럼 | 타입 / NULL | 의미 |
|---|---|---|
| food_id | 기존 BIGINT PK 유지 | ITEM 식별자, 기존 URL·이력 연결 보존 |
| master_id | BIGINT NOT NULL, FK RESTRICT | 소속 음식 |
| stock_batch_id | UUID NOT NULL | 원래 구매·등록분. 신규 유입마다 생성, 분리 시 공유 |
| parent_item_id | BIGINT NULL, 자기 FK RESTRICT | 직접 분리된 원본 ITEM |
| quantity_amount | 기존 NUMERIC(12,3) NULL | 현재 잔량. 타입 축소 없음 |
| quantity_unit | 기존 VARCHAR(10) NULL | 실제 재고 단위 |
| quantity_text | 기존 VARCHAR(50) NULL | 구조화 값의 표시 문자열 또는 기존 원문 |
| status | VARCHAR(20) NOT NULL | ACTIVE / DEPLETED |
| exclude_expiry_warning | BOOLEAN NOT NULL DEFAULT FALSE | 해당 ITEM 기한 경고 제외 |
| exclude_opened_warning | BOOLEAN NOT NULL DEFAULT FALSE | 해당 ITEM 개봉 경고 제외 |
| version_no | BIGINT NOT NULL DEFAULT 0 | 상태 변경 충돌 검증 |

나머지 기존 컬럼을 유지한다: storage_type, capacity_text, expired_at, sell_by_at,
purchased_at, opened_at, frozen_at, source_type, source_memo, freeze_type, memo,
created_at, updated_at. 기존 날짜·출처·위치 제약은 새 모델과 함께 검증한다.

food_name/category는 MASTER로 읽기·쓰기 책임을 옮긴다. 새 앱과 함께 전환하며
필요한 값의 복사 후 ITEM의 중복 컬럼은 제거한다. 구버전 코드 호환용 이중 쓰기는 하지 않는다.

수량·상태 제약:
- 비구조화 예외: amount/unit 모두 NULL이고 ACTIVE. 기존 quantity_text를 그대로 표시한다.
- 구조화 ACTIVE: amount > 0, 유효한 unit 존재.
- 구조화 DEPLETED: amount = 0, 유효한 unit 존재.
- 단위는 공백만 허용하지 않고 최대 10자. 기존 숫자 상한 유지.
- 새 등록·추가 구매는 0 초과. 소비·폐기 처리량도 0 초과.
- amount/unit 중 하나만 NULL인 상태는 금지한다.
- 구조화 값 변경 시 Service가 quantity_text를 재생성한다.

분리 제약:
- 0 < 분리량 < 현재 잔량. 전량 개봉·이동은 동일 ITEM 수정.
- 부모와 자식은 같은 master_id, stock_batch_id, 단위를 유지한다.
- 분리량만큼 기존 ITEM을 줄이고 새 ITEM을 하나 만든다. 같은 트랜잭션으로 처리한다.
- 자식은 기존 구매 정보와 날짜를 복사한 뒤 요청한 관리 조건만 변경한다.
- 신규 자식만 부모를 지정하고, 재부모 지정은 하지 않는다. 자신을 부모로 지정하지 못한다.
- stock_batch_id 자체는 유입 수량을 나타내지 않는다. 최초 수량은 유입 이력이 근거다.

인덱스: master_id, stock_batch_id, parent_item_id 및 현재 ACTIVE 날짜 검색 인덱스를 유지·검토한다.

### 5. food_history 유지·확장

기존 PK와 food_id FK, action_type, previous_storage_type, new_storage_type,
quantity_text, created_at, changes_text를 보존한다. 기존 행의 원문은 재작성하지 않는다.
(memo 컬럼은 상수 기록 외 용도가 없어 2026-09-17 V16에서 제거했다. 아래 정리 작업 참고.)

| 추가 컬럼 | 타입 / NULL | 의미 |
|---|---|---|
| operation_id | UUID NULL | 신규 작업에서 필수. 같은 작업의 여러 History를 연결 |
| processed_quantity_amount | NUMERIC(12,3) NULL | 해당 ITEM에 적용한 양 |
| quantity_unit | VARCHAR(10) NULL | 당시 단위 |
| before_quantity_amount | NUMERIC(12,3) NULL | 작업 전 해당 ITEM 잔량 |
| after_quantity_amount | NUMERIC(12,3) NULL | 작업 후 해당 ITEM 잔량 |
| snapshot_version | INTEGER NULL | 신규 스냅샷 형식 버전 |
| before_snapshot | JSONB NULL | 작업 전 구조화 원본 값 |
| after_snapshot | JSONB NULL | 작업 후 구조화 원본 값 |
| reversal_of_history_id | BIGINT NULL, 자기 FK RESTRICT, UNIQUE | 취소 대상 |
| before_item_version | BIGINT NULL | 작업 전 ITEM 버전 |
| after_item_version | BIGINT NULL | 작업 후 ITEM 버전 |

기존 행은 신규 컬럼 NULL을 허용한다. operation_id가 있는 신규 행에는 버전 및
작업별 필요한 수량·스냅샷 필드의 정합성을 강제한다. 허용할 action_type과 기존
위치 전이 CHECK를 함께 갱신한다. CREATE/UPDATE 분기만 추가해서 끝내지 않는다.

신규 작업 종류:
CREATE, ADD_STOCK, UPDATE, STRUCTURE_QUANTITY, CONSUME, DISCARD,
OPEN, FREEZE, MOVE, SPLIT, CANCEL.

- STRUCTURE_QUANTITY는 과거 자유 입력을 현재 잔량으로 확인하는 작업이다. 구매량 증가 없음.
- CREATE/ADD_STOCK은 실제 신규 유입에만 사용한다. 분리 자식에 CREATE를 남기지 않는다.
- 부분 개봉·이동은 같은 operation_id의 SPLIT 이력 2건으로 기록한다.
  부모 이력은 n → n-q, 자식 이력은 생성 전 NULL → q이다.
  스냅샷에 분리 목적(OPEN/MOVE 등), 원본·신규 ITEM ID, 구매분 ID를 보존한다.
- SPLIT의 두 이력에 같은 양이 있어도 구매·소비·폐기량에 더하지 않는다.
- 전량 개봉·이동은 OPEN/MOVE/FREEZE 이력 1건이다.
- CANCEL은 원본 이력을 삭제하지 않고 reversal_of_history_id로 연결한다.
- JSONB에는 표시 문장 대신 안정된 필드명, 숫자, 날짜, enum 코드와 명시적 null을 저장한다.
- 당시 음식명과 master_id, 구매분 연결은 스냅샷에도 보존한다.
- 현재 소속 MASTER는 ITEM에서 조회한다. History에 현재 master_id를 중복 FK로 두지 않는다.
- 과거 등록 snapshot-v1과 수정 changes_text 표시를 계속 지원한다.

집계:
- 신규 유입량은 CREATE/ADD_STOCK만 포함한다. SPLIT/OPEN/MOVE/UPDATE/CANCEL은 유입이 아니다.
- 출처에 직접 조리·배달 잔반도 있으므로 전체는 '총등록량/총유입량'이다.
  실제 '총구매량'은 이 중 당시 source_type=PURCHASE인 기록만 집계한다.
- 소비·폐기는 원본 처리량에서 유효한 취소를 제외해 계산한다.
- 숫자가 없는 과거 이력은 구매량 0으로 간주하거나 현재 잔량으로 역산하지 않는다.
- 물리 삭제된 기록은 집계에서도 사라진다. 삭제는 통계 영구 보존과 양립하지 않는 명시적 기능이다.

### 6. 취소와 충돌

- 같은 ITEM의 마지막 유효 CONSUME/DISCARD만 후보이며 이미 취소된 이력은 제외한다.
- 이후 소비·폐기·수량 정정·단위 변경·구조화·이동·개봉·분리가 있으면 차단한다.
- 소비 후 소비 후 취소처럼 연결된 후속 수량 작업이 있는 과거 처리는 첫 버전에서 취소하지 않는다.
- 음식명·메모·경고 설정 등 수량과 충돌하지 않는 변경은 유지한다.
- 날짜 변경도 실제 개봉·이동 등의 상태 변경에 해당하는지 구분한다.
- 취소 시 현재 잔량에 원래 처리량을 더하고 quantity_text/status만 재계산한다.
- snapshot 전체 복원 금지. 날짜·위치·개봉·음식명·메모·경고 설정을 덮어쓰지 않는다.
- 현재 화면 버전 검사와 취소 가능성 검사는 별개다. 버전 불일치는 새로고침 후 재검증한다.
- ITEM 갱신, 취소 이력 INSERT, 중복 취소 방지는 한 트랜잭션이다.

version_no는 모든 ITEM 변경 시 증가시킨다. 수량 이상 변경은 action_type만 보지 않고
전후 구조화 필드로 확인한다. 기존 updatedAt 비교를 버전 기반으로 전환하되 어느 경로도
충돌 검증을 빠뜨리지 않는다.

operation_id는 이력 연결 키이지 그것만으로 중복 제출 방지를 보장하지 않는다.
신규 쓰기 요청에 재사용 가능한 요청 토큰과 요청 내용 식별값을 적용하고, DB UNIQUE로
완료 결과를 한 번만 확정하는 별도 operation/request 기록을 둔다. 같은 토큰·다른 내용은 거부한다.
엑셀은 같은 업로드의 확인 재시도뿐 아니라 파일 재업로드의 신규 행 중복도 별도 식별한다.
이 기록의 세부 컬럼과 업로드 행 식별 규칙은 업로드 구현 설계에서 구체화한다.

### 7. 물리 삭제

- 웹의 오등록 정정 도구로만 제공한다. 소비·폐기와 별도 기능이다.
- 대상 ITEM 수·History 수와 다른 재고에 미치는 연결 영향을 먼저 보여주고 재확인한다.
- 실제 실행 시 대상 집합·버전·건수를 다시 검증한다. 확인 뒤 변경되면 재확인한다.
- MASTER 잠금을 기준으로 추가 등록·분리·통합·삭제와의 경쟁을 직렬화한다.
- History 삭제 → ITEM 삭제 → 요청한 경우 MASTER 삭제 순서로 같은 트랜잭션에서 처리한다.
- MASTER/ITEM/History FK는 RESTRICT 유지. 광범위한 CASCADE를 사용하지 않는다.
- 취소 History를 원본 History보다 먼저 지우는 등 자기 FK 의존성을 처리한다.
- ITEM만 삭제할 때 비어 있는 MASTER를 자동으로 삭제하지 않는다.

분리 관계 처리 제안:
- 부모 ITEM만 삭제해도 살아 있는 자식 ITEM을 자동 삭제하지 않는다.
- 삭제 전 미리보기에 영향을 받는 자식 연결 건수를 표시한다.
- 생존 자식의 parent_item_id를 명시적으로 NULL 처리하고 버전을 갱신한 후 부모를 삭제한다.
- 생존 자식의 stock_batch_id와 당시 분리 snapshot은 유지한다.
- 같은 operation_id라는 이유로 생존 ITEM의 이력을 함께 삭제하지 않는다.
- 부모 등록 이력이 삭제되면 구매분 최초 수량이 확인 불가능할 수 있음을 인정한다.
  생존 자식 수량을 신규 구매량으로 대체하지 않는다.

위 생존 자식 처리 방식은 확정된 삭제 범위를 구현하기 위한 제안이며 실제 UI에 드러내야 한다.

### 8. Flyway 전환 전략 — 테스트 단계에 맞춰 단순화

원칙: 이미 적용된 V1~V6은 수정하지 않는다. 실제 최신 migration 번호를 구현 시 다시 확인한다.

전체 전환은 기능 단계별 migration과 새 앱을 함께 적용한다. V7은 음식 그룹·병합 기반을 담당하고,
수량·분리·취소 확장은 후속 migration으로 이어간다. 각 단계에서 구버전 앱과 동시 운영하지 않는다.
- MASTER 생성, ITEM 확장, History 구조화 컬럼, 상태·수량 제약과 인덱스를 함께 적용한다.
- 기존 앱은 전환 중 중지한다. 구버전 앱과의 동시 운영은 지원하지 않는다.
- 간단하게 보존할 수 있는 기존 재고는 ITEM 1건당 MASTER 1개로 연결한다. 이름 기준 통합 금지.
- 기존 ID와 changes_text는 유지할 수 있으면 유지한다. 자유 문자열이나 과거 이력의 정교한 역산은 하지 않는다.
- food_name/category를 MASTER로 복사한 후 ITEM 중복 컬럼을 제거한다.
- ACTIVE 비구조화 재고를 유지하는 경우 기존 원문·NULL 쌍과 수동 구조화 정책을 적용한다.
- 과거 소진 상태·모순된 테스트 데이터 때문에 복잡한 변환이 필요하면 별도 호환 계층을 만들지 않는다.
  실제 대상 데이터를 확인하고 테스트 데이터 초기화 경로를 선택할 수 있다.
- 데이터 초기화는 별도 명시적 실행으로 구분한다. 앱 시작 때 자동으로 데이터를 지우지 않는다.
- 새 빈 테스트 DB에서 V1~새 migration 전체 실행과 새 시나리오 fixture를 검증한다.
- 보존 전환을 선택한 경우 해당 기존 데이터 fixture에서도 migration을 검증한다.
- 운영용 단계적 배포·복잡한 rollback 자동화는 실사용 배포 단계에서 필요에 맞춰 마련한다.

### 9. 구현 완료 검증 기준

- 보존 전환 선택 시 기존 ITEM/History ID·건수·원문 보존, MASTER 매핑 누락 없음, 자동 이름 통합 없음.
- 신규 입력/기존 비구조화 조회와 구조화, 0 소진, 수량 초과·중복 처리 방지.
- 두부 2모 → 1모 개봉 → 0.5모 소비 → 나머지 폐기 → 취소.
- 기존 구매분 1모 + 신규 구매분 2모에서 오래된 1모만 폐기.
- 분리 후 구매량 증가 없음, 분리 전후 총수량 보존, 다단계 부모 연결.
- 메모 변경 보존 취소, 개봉/이동/분리 후 취소 차단, 이중 취소 방지.
- 삭제의 자기 FK·생존 자식·취소 이력 처리, 확인 이후 변경 시 재확인, 실패 시 전체 복원.
- 과거 snapshot-v1/changes_text와 신규 JSONB의 이력 화면 호환.
- 엑셀 오류 한 건 시 전체 미반영, 미리보기 뒤 MASTER 변경·삭제 재검증, 중복 업로드 방지.
- 기존 날짜 정책·쪼코 이미지 선택 정책 유지. 테스트 데이터 보존 여부는 선택한 전환 경로에 따른다.

목록·상세의 수량 요약과 후속 액션 UI, 대표 단위 매핑, 업로드 식별 방식은 이 정책 위에서 구체화한다.
### 일괄 등록 양식 정리 (2026-09-16)

신규 등록·추가 등록을 별도 시트로 제공한다. 음식 묶음은 일괄 등록에서 지원하지 않으며 신규 등록은 행마다 하나의 음식을 만든다. 추가 등록은 기존 음식 선택이 필수다. 입력 제한은 두 시트 합계 500개이며 기본 100개 입력 행 이후에는 복사해서 사용한다. 상세 열 구성과 검증 규칙은 [일괄 등록 양식](BULK_REGISTRATION_DRAFT.md)을 참고한다.
### 작업 마무리 — 2026-09-16

- 신규 등록·추가 등록·일괄 등록을 한 줄의 동일한 선택 UI로 제공한다. 엑셀 양식은 사용자 원본 서식을 보존하고 기존 음식 목록·수식·유효성 검사만 갱신한다.
- 일괄 등록 안내 말투와 자동 냉동 보정의 두 가지 안내 조건을 확정했다. 상세 정보 레이아웃, 부분 화면 갱신, 필터 빈 결과의 버튼 표시를 정리했다.
- 개별 구매 카드 날짜와 일괄 등록 상세 정보의 폰트·좌측 정렬·라벨 간격을 통일했다. 구체적인 규격은 UI_DESIGN, 양식·보정 정책은 BULK_REGISTRATION_DRAFT에 유지한다.
- 기존 로컬 8080만 사용한다. 별도 8082 앱은 다시 실행하지 않는다. 통계·종료 항목 이동·운영 배포 방식 결정은 후속 범위다.

### 최종 확인 및 커밋 준비

- 사용자가 자동 업로드·검증, 중복 확인과 등록 버튼, 누락 집계, 안내 문구·폰트, 체크박스 색상과 구매 목록 UI까지 확인했다.
- 전체 Java 473개·JavaScript 41개 통과, 실패·오류·건너뜀 0. JAR 빌드 통과.
- 임시 양식 초안·다운로드 비교 파일·캡처·일회성 수정 및 검증 스크립트를 폐기했다. 배포용 원본 엑셀, 정식 회귀 테스트 및 백업은 유지한다.
- 당시 다음 작업이었던 검증 문구 유형 분류는 후속 작업에서 일괄 등록 범위로 구현했다. 기준은 BULK_REGISTRATION_DRAFT를 따른다.

### 개인 휴대폰 사용 준비 — 후속 작업

- 사용자는 PC를 계속 켤 수 없으며 LTE/5G에서도 접속해야 한다. 무료 서버로 시작하고 Oracle Cloud 신규 가입·카드 인증이 가능하다고 확인했다.
- Oracle 가입 실패 후 Render Free + Neon Free(Singapore)로 확정했다. Neon production 연결과 요청한 빈 neon.ts 배포를 완료했다. Render용 단일 개인 로그인·CSRF·메모리/연결 제한을 구현했다. 실제 Render 배포·모바일 접속 검증은 미완료다.
- 검증 문구 다음 순서는 서버 확보·배포·백업·실기기 확인이다. 수량 분리·삭제·통계는 최초 휴대폰 사용 이후로 미룬다.

### 운영 반영 확인 — 2026-09-17

f08d701 Render Live, Neon V15 성공, choconuna/testuser USER 계정 생성과 두 계정의 HTTPS 로그인·주요 화면 조회·로그아웃을 확인했다. 실데이터 등록·교차 계정 변경과 실기기 접속은 별도 검증 대상이다. 반복 운영 절차는 [운영 안내](OPERATIONS.md)를 따른다. 후속 여백·운영 도구 변경과 다른 PC 준비 절차는 OPERATIONS.md를 따른다.

### 코드 정리 작업 — 2026-09-17

과잉 구현 리뷰(fb9979b, 68ab6fa)로 기능 변화 없이 정리했다. 전 항목 통합 테스트로 검증 완료.

- **쪼코 원본 PNG 77개(239MB)를 `src-assets/choco/`로 이동.** 화면은 `assets/choco/web/v1/`의 WebP만 서빙하므로 jar와 배포 아티팩트에서 제외했다. WebP 재생성 시 `scripts/build-choco-web.py`가 새 경로를 읽는다.
- **food_history.memo 컬럼 제거(V16).** 값이 '음식 등록'/'음식 수정' 상수뿐이고 action_type이 같은 정보를 가지며, 어떤 화면·조회 SQL도 읽지 않음을 확인했다(food_history를 읽는 SELECT는 HistoryDao.findRecent와 FoodQuantityDao 2건뿐, 셋 다 memo 미포함). food_item.memo(음식 메모)는 별개 컬럼으로 유지한다. 기존 행의 memo 값은 컬럼 drop과 함께 소멸한다.
- **테스트 전용 메서드 제거.** `FoodMasterService.groups(storage)` 단축형, `FoodQuantityService.apply` 5-인자, `BulkWorkbook.template()` 무인자, `registrationQuantity` 서비스·DAO. 테스트는 전체 시그니처 호출 또는 jdbc 직접 조회로 전환했다. 롤백 테스트의 실패 주입 제약은 memo 대신 `CHECK (action_type <> 'CREATE')`를 쓴다.
- **소규모 정리.** `HistoryService` 패스스루 삭제(컨트롤러가 `HistoryDao` 직접 사용), `WebConfiguration` 삭제(`spring.mvc.format.date: iso` yml 1줄로 대체), 죽은 생성자·enum·CSS 규칙·도달 불가 JS 분기 제거, redirect URL 헬퍼는 `FoodQuantityController.url` 하나로 공유.
