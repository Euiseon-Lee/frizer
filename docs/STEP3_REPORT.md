# FR!ZER STEP 3 설계 및 구현 보고서

작성일: 2026-09-13. 범위: MASTER·ITEM 재고 구조, 소비·폐기·분리·취소·삭제와 엑셀 등록·추가 구매.

상태: **음식 그룹·전체/개별 이동·추가 등록·수량 입력 및 전량/부분 소비·폐기·제한적 취소·종료 탭 구현 / 부분 분리·통계 등 후속 기능 미구현.** 사용자 확정 정책과 구현 제안을 구분해 기록한다.
STEP 3의 설계부터 구현·검증 결과까지 이 파일을 기준 문서로 갱신한다.
STEP 1·2 보고서는 완료 당시의 기록으로 유지한다.
엑셀 양식·처리 흐름의 세부 초안은 [일괄 등록 초안](BULK_REGISTRATION_DRAFT.md)에서 관리한다.
정책과 출시 범위는 이 STEP 3 보고서가 기준이며 보조 초안도 같은 결정에 맞춰 갱신한다.

## 최종 상태와 보류 사항

2026-09-15 커밋 준비: 수량 표시/입력 크기와 안내 간격(4px)을 정리하고, 소비 질문을 '이 구매 항목을 이렇게 기록할까?'로 변경했다. 처리 완료 알림 닫기, 구매 목록의 보관 중/종료 탭과 빈 쪼코 카드를 추가했다. 최신 화면 규격은 [수량 처리 기록](ITEM_QUANTITY_DESIGN.md)을 따른다. 실제 커밋·푸시는 아직 하지 않았다.

2026-09-15 UI 후속: 확인 화면의 **기준 수량 / 먹은 수량·버릴 수량** 직접 입력과 부분 소비·폐기를 추가했다. 버튼은 실온/냉장실 카드색 테두리·흰 배경·hover 채움이며 확인 문구는 '응, 좋아!'/'응, 버리자!'. V12에서 수량 차이 제약을 확장한다. 부분 분리·통계는 후속 범위이며 상세는 [수량 처리 기록](ITEM_QUANTITY_DESIGN.md)을 따른다.

이 후속 변경 검증: 전체 Java 423개·bootJar 및 JS 30개 통과. 기존 8082 앱에 반영했고 사용자 재고에 처리 요청을 보내지 않고 입력·표시를 확인했다.

2026-09-15 후속 구현: **먹었어 / 버렸어**, 전량 처리 확인과 취소, 종료 탭 및 경고 체크박스 이동을 구현했다. 마지막 활성 재고 처리 후에도 그룹과 이력을 보존하며 종료 그룹에 다시 구매 항목을 추가할 수 있다. 최신 확정 정책·저장 계약·검증은 [전량 처리 기록](ITEM_QUANTITY_DESIGN.md)을 따른다. 아래 '소비·폐기 미구현' 문구는 이전 단계 기록이다.

장기 추적은 사용자 지정 음식 그룹을 기준으로 하며 별도 연결 계층은 도입하지 않는다. 구매 당시 수량을 단위별로 집계하는 후속 방향을 확정했다. 통계·부분 수량·종료 항목 개별 이동·물리 삭제 UI는 미구현이다.

최신 전체 검증: Java 406개 및 bootJar 통과, JS 29개 통과. 상태를 세 가지에서 두 가지로 바꿔 기존 조합 검사 18개가 줄고 신규 수량 검사 14개가 추가됐다. 이미지 해시 불일치는 그대로 보존한다. 사용자 DB/8080에는 적용하지 않았다.

2026-09-15 다음 단계: 개별 구매 항목 하나의 소속 이동·새 음식 분리를 구현했다. 마지막 연결 항목 이동 후 빈 그룹을 정리하고 이력은 보존한다. 수량 일부 분리와는 다른 기능이며 [구현 기록](ITEM_MOVE_DESIGN.md)에 계약과 검증을 정리한다.

해당 단계 검증: 전체 Java 410개와 bootJar 통과, 이동·선택·등록 JavaScript 검사 통과. 쪼코 선택기 검사는 별도로 변경된 이미지의 원본 해시 불일치로 실패했다. 8082 화면 확인 완료, 8080은 사용자 재시작 필요.

2026-09-14 마감 재검증: 최종 UI 변경 포함 Java 389개, 실패·오류·건너뜀 0, JavaScript 4종 및 bootJar 통과.

최종 화면 규격과 검증은 [작업 정리](SESSION_SUMMARY.md)를 따른다. 아래 구현 기록의 과거 문구·색상·배치는 이후 사용자 요청으로 대체된 이력을 포함한다.

- 개별 구매 목록 날짜는 2열이며 네 날짜 영역만 1pt 축소한다. 날짜와 경과 일수 사이에는 공백 한 칸을 유지한다.
- 구매 목록 버튼은 이동/추가 5:5, 전체 목록으로는 다음 행 전체 너비다. 경고 날짜 라벨 색은 #D93636으로 복원했다.
- 완료 영역 전체 카드의 하트 아이콘은 같은 크기의 강아지 얼굴로 교체했다.
- 긴 이름 헤더와 WARNING 필터·전체 보기 연결은 2026-09-15 구현했다. 제목은 최대 3줄·최소 16px이며, 초과 시 눌러 펼친 전체 이름은 12px로 표시한다. 펼친 영역을 다시 누르면 닫힌다. 상세 검증은 [작업 정리](SESSION_SUMMARY.md)를 따른다.
- 2026-09-15: 홈 WARNING 전체 보기, 보관 위치·경고 조건 조합과 구매 화면 왕복 시 필터 유지를 구현했다. 관련 통합 테스트 121개와 bootJar 통과. 전체 테스트 재실행 결과는 아니며 상세 검증은 [경고 연결 기록](HOME_WARNING_COMPACT_REVIEW.md)을 따른다.

## 구현 결과 및 단계별 기록

- 반복 UI 수정의 최종 상태와 미구현 논의 항목은 [작업 정리](SESSION_SUMMARY.md)에 정리했다. ‘음식 이동’은 기존 음식의 구매 항목 전체 이동이며 개별 선택 이동/분리는 후속 범위다.

- 히스토리 화살표·동일 수량 이력 제외·이동 안내 줄바꿈·구매 목록 버튼 5:5 배치를 정리했다. [검증 및 일회성 개발 데이터 정리](HISTORY_ACTIONS_REVIEW.md) 참고. 전체 Java 389개 통과. 과거 기록 전용 런타임 보정 로직은 추가하지 않았다.

- 날짜 경과 표기, 이동 문구, 두 줄 선택 목록, 최종 음식명 정렬, 취소 hover를 반영했다. [이동 UI 검증 결과](FOOD_MOVE_UI_REVIEW.md) 참고. 전체 Java 388개와 이동/선택 목록 JavaScript 검사 통과.

- 최신 홈 WARNING은 한 줄·최대 3개 표시로 변경했다. 전체 집계·정렬·날짜 판정은 유지하고 히스토리의 이전 등록 안내만 제거했다. 관련 통합 테스트 90개 통과. 기존 WARNING 전체 필터가 없어 전체 보기 연결은 미구현이며 [최신 화면·검증 및 연결 작업](HOME_WARNING_COMPACT_REVIEW.md)에 기록했다.

- 홈 WARNING 카드의 색상·세로 정보 배치·날짜 문구와 합치기 이력 안내를 정리했다. [화면 및 검증 보고서](HOME_WARNING_REVIEW.md)를 참고한다. 최신 전체 Java 379개와 bootJar 통과. 경고 대상·정렬·건수·날짜 계산·이미지 선택 정책은 유지했다.

- 히스토리에 합치기 기록을 추가했다. 기존 food_merge_receipt를 개별 구매 이력과 합쳐 최근순으로 조회하므로 과거에 완료한 합치기도 표시되고 재시도로 중복 기록이 생기지 않는다. '합치기 / 원래 음식 → 합친 음식 / 개별 항목 N건 / 처리 시각'을 표시한다. 새 migration이나 과거 이력 재작성은 없다.
- 과거 이름과 현재 음식명이 다르면 '현재 ‘음식명’의 개별 구매 항목으로 이동해.'를 표시한다. 과거 등록·수정 기록은 당시 이름과 원문을 보존하고 해당 ITEM에 연결한다. 합치기 기록은 현재 MASTER의 개별 구매 목록에 연결하며 A→B→C 이후 A→B 기록도 C를 가리킨다. 현재 음식이 사라진 경우 전체 목록으로 연결한다. 이름 수정만으로 합치기 기록을 추정 생성하지 않는다.
- 홈 최근 5개와 히스토리 최근 100개는 합치기를 포함한 통합 제한이다. 합치기 기록은 음식 단위 처리 영수증이므로 ITEM 이력 보존 건수 계산에는 섞지 않는다. 사용자 실행 앱을 재시작하면 새 조회가 적용된다.
- 히스토리 보완 후 전체 Java 378개(기존 372 + 합치기 이력 6), 실패·오류·건너뜀 0 및 bootJar 성공. 일회용 DB/18083에서 긴 이름→두부 기록·현재 연결 안내·두 종류 링크·홈 기록과 320px 배치를 확인했다. 사용자 8080은 HTTP 200 상태를 유지했고 데이터 변경·재시작하지 않았다.
- 전체 음식 카드의 구매 항목 3열에서 위치·수량·단위·경고 아이콘 묶음을 열별로 정렬했다. 첫 열은 왼쪽, 둘째 열은 가운데, 셋째 열은 오른쪽이며 네 번째 항목부터 같은 규칙을 반복한다. 줄바꿈 시에도 해당 열의 정렬을 유지한다. CSS 변경으로 별도 Java 테스트는 추가하지 않았다.
- 2026-09-14 구현 범위 검증 보강: [테스트 목록 및 결과](STEP3_TEST_MATRIX.md)에 경계·오류·순차/동시 요청·실패 롤백·브라우저 흐름을 연결하고 [Java 실행 목록](STEP3_TEST_CASES.md)을 보존했다. 기존 192개 + 신규 180개 = Java 372개, 실패·오류·건너뜀 0, bootJar 성공. 등록 UI JavaScript 25개와 이미지·합치기 검사도 통과했다.
- 이번 검사에서 기존 음식 추가의 잘못된 수량 형식 처리 중 오류 화면 500, 선택 변경 후 취소 링크 불일치, 서로 다른 병합에 동일 UUID를 동시에 사용했을 때의 서버 오류를 재현하고 수정했다. 각각 회귀 검사를 추가했다.
- 별도 일회용 DB와 18083 앱에서 신규 등록→기존 음식 검색·추가→출처 수정→병합→이력 보존을 확인했다. 6개 화면 × 320/390/448px의 18조합에서 DOM 가로 넘침이 없었고 320px 병합 상하단·구매 카드 배치를 시각 확인했다. 검증용 앱·DB는 종료했으며 기존 사용자 DB는 변경하지 않았다.
- 신규 음식 등록의 중복 제출 방지를 구현했다. 화면별 UUID를 발급하고 클릭/Enter 제출 직후 버튼을 잠근다. V9 food_registration_receipt의 요청 ID 고유 제약으로 동일 ID·동일 입력 재전송은 기존 결과를 반환하고 동일 ID·다른 입력은 거부한다. 요청 확보·음식·이력·결과 확정은 한 트랜잭션이며 실패 시 같은 ID로 재시도할 수 있다. 다른 ID로 같은 이름을 등록하는 것은 계속 허용한다. 기존 음식 추가의 서버 버전 검사도 유지한다.
- 사용자 문구는 '해줘'로 통일했다. '이미 보낸 요청이야. 다시 확인해줘.', '음식을 합치지 못했어. 다시 시도해줘.'를 적용하고 UI 안내의 '병합'을 '합치기' 표현으로 변경했다.

- 화면 후속 정리: 이동 버튼은 전체 목록으로 / 개별 목록으로, 홈 최근 기록은 수정한 정보 N건으로 표시한다. 전체 음식 헤더는 쪼코랑 같이 찾아보자! / 뭐가 있는지 볼까? / 궁금한 음식은 하나씩 눌러 봐.로 확정했다.
- 개별 구매 목록·항목의 날짜 라벨을 굵게 표시한다. 입력된 소비기한 날짜는 임박 여부와 관계없이 굵게 표시한다. 목록 날짜 영역 양옆 여백은 8px이며 경고 아이콘은 문자 줄 중앙 정렬에 1px 아래 시각 보정을 적용한다.
- 음식 합치기 본문을 선택·미리보기 통합 화면으로 구현했다. 공통 헤더는 유지하며 확정 문구(옮길 음식, 합친 후 음식명, 이동할 개별 구매 총 건수, 같이 이동할 이력 총 건수, 확인했어, 합치자!)를 반영했다.
- 대상 변경 시 기존 GET 미리보기 HTML에서 서버가 계산한 결과와 버전·요청 UUID를 가져온다. 기존 GET/POST 계약과 서비스의 잠금·버전·영수증·데이터 보존 의미는 유지한다. 구매·이력 총 건수는 원래 음식에서 이동할 건수이며 대상의 기존 건수를 합산하지 않는다.
- 이전 요청 취소와 응답 순서 검증, 실패 재시도, 선택 해제, 제출 중 선택·중복 제출 차단, 브라우저 기록 복귀 시 재조회가 적용된다. 실행 실패는 기존 서버 이동·안내를 유지하며 자동 재실행하지 않는다.
- 검증: FoodMergeIntegrationTest 11개 통과, food-merge.test.cjs의 응답 역전·재시도·불일치·중복 제출·기록 복귀 검증 통과. 실제 8082 브라우저에서 선택/변경/해제 및 320px·390px 가로 넘침 없음, 하단 동작 접근을 확인했다.
- 음식 합치기 성공 후에는 `/inventory`의 전체 음식 목록으로 이동하고 기존 성공 안내를 표시한다. 동일 요청 재제출의 성공 응답도 같은 경로를 사용한다.
- 개별 구매 목록의 날짜 경고는 개별 구매 항목과 동일한 기준 날짜와 경고 조건·글씨 색·아이콘 색을 사용한다. 경고 아이콘은 해당 날짜 오른쪽에 표시한다.
- 2026-09-14: 음식별 목록·구매분별 상세와 명시적 병합을 1차 구현 범위로 분리했다.
- V7은 food_master 생성, 기존 ITEM 1건당 MASTER 1개 연결, 음식명·분류 책임 이전을 수행한다. 이름이 같아도 자동 통합하지 않는다.
- 합치기 화면에서 원래 음식의 이동할 구매·이력 건수를 표시하고, 대상 음식의 이름·분류·기본 설정을 유지한다.
- 병합 시 ITEM ID와 수량·날짜·상태, 기존 History 원문을 보존하고 소속만 변경한다. 비워진 원본 MASTER는 삭제한다.
- MASTER 순서 잠금·버전 검증·요청 UUID 영수증으로 동시 변경과 재제출을 처리한다. 영수증 저장까지 하나의 트랜잭션이다.
- 이력에는 당시 이름 보관 컬럼을 추가했다. 기존 등록 snapshot-v1에 이름이 있으면 사용하고, 그 외 과거 기록은 전환 당시 이름을 저장한다. 이미 유실된 과거 이름을 추정하지 않는다.
- 기존 등록·수정은 새 MASTER 구조로 전환했다. 이름·분류는 공유 변경이며 다른 ITEM의 오래된 수정 화면도 무효화한다.
- 이번 1차에서는 기존 ITEM 상태와 updatedAt 충돌 검사를 유지한다. 아래 stock_batch_id, 분리·수량 이력·ITEM version_no·소진 상태 전환은 후속 migration으로 구현한다. 구버전용 중복 이름 컬럼은 유지하지 않는다.
- 소비·폐기·취소·오등록 삭제·엑셀과 경고 기본 설정 편집 UI는 아직 구현하지 않았다. 기존 음식 추가 구매는 아래 후속 구현에 반영했다.
- 로컬 DB는 전환 전에 `build/backups/frizer-before-v7.dump`로 백업했다(Git 제외). V7 적용 후 기존 재고 1건·이력 1건을 유지하고 MASTER 1건으로 연결했다. 실제 재고를 병합하거나 초기화하지 않았다.
- V7 전환 당시 구버전 앱을 중지했다. 현재 Docker 확인 앱은 8082, 사용자 IntelliJ 앱은 8080을 사용한다.
- 다른 음식이 없으면 합치기 버튼을 비활성화하고 합치기 URL 직접 접근도 상세로 돌린다. 개별 구매 목록은 바깥 패널 없이 구매별 카드를 표시하며 합치기 화면의 돌아가기는 텍스트 버튼이다.
- 전체 음식 카드의 첫 구분선 아래는 3열이다. 각 칸에는 위치 태그·수량·경고 아이콘만 표시하고 네 번째 항목부터 다음 행에 배치한다. 항목별 구분선은 제거했다.
- 화면 명칭은 '전체 음식' → '개별 구매 목록' → '개별 구매 항목'으로 통일한다. 전체 음식 제목 옆에는 음식 그룹 개수를 표시한다. 개별 구매 항목의 '개별 목록으로' 버튼은 해당 음식의 `/foods/{masterId}`로 이동하며, 중복된 '같은 음식의 구매분 보기' 링크는 제거했다.
- 개별 구매 목록 안내는 '항목을 선택하면 자세한 정보를 확인하고 수정할 수 있어.'이며 왼쪽 여백은 8px이다. 카드 하단에는 등록 일시와, 등록 이후 변경이 있을 때만 수정 일시를 표시한다. '상세 보기' 문구는 제거했다.
- 화면의 항목 수는 '개별 구매 N건'으로 오른쪽 정렬해 표시한다. 이는 해당 음식 아래의 개별 구매 항목 수이며 수량 합계가 아니다. 수정·병합 안내도 '개별 구매 항목'으로 통일한다.
- 현재 수량은 모든 표시 영역에서 숫자와 단위를 붙인다(예: `5pieces`, `2모`, `0.5팩`). 전체 음식·개별 구매 목록의 마크업 공백, 구매 카드의 CSS 간격, 상세 공통 표시 함수의 공백을 제거했다. 이력의 구조화 등록 수량은 기존에도 붙여 표시한다. 저장값·과거 이력·비구조화 원문은 변경하지 않는다.
- 수량 표기 변경 후 관련 검사 5개(수량 경계·소수 4경우 × 전체 목록/구매 목록/상세/이력, 사용자 단위 이스케이프·기존 원문 보존)와 bootJar가 통과했다. 이번에는 전체 372개 재실행 및 브라우저 시각 재검수는 수행하지 않았다. build 테스트 결과는 최신 부분 실행을 반영한다.
- 상세 표기 추가 확인: 실행 중인 8080 앱은 이전 Java 표시 함수 때문에 여전히 `1 개`를 반환했다. 상세 템플릿에서 숫자와 단위를 직접 이어 표시하도록 변경해 UI 원본 새로고침만으로 반영했다. 실제 8080의 구매 항목 4건 HTTP 응답에서 변경 전 `5 모/2 개/1 개/2 개`, 변경 후 `5모/2개/1개/2개`를 확인했다. 데이터 변경·앱 재시작 없이 검증했다.
- 개별 구매 카드의 수량과 날짜 영역 사이 구분선을 제거했다. 등록·수정 일시 영역의 구분선은 유지한다.

별도 UI 후속 작업(2026-09-13): 쪼코 기존 21장 전체 후보와 영역별 shuffle-bag를 적용한 뒤,
추가 ZIP 20개 행을 ADDED 15 / REUSED 5 / 누락 0으로 처리했다. 현재 이미지 총 36장,
일반 헤더 26장, 오류 WAITING 5장, 프로필 2장 중 세션 선택 정책이다.
이미지 선택기·Java 177개·bootJar 및 모바일 브라우저 확인을 통과했다.
상세는 [이미지 정책 보고서](ui-v5-addon/FINAL_POLICY_REPORT.md)에 기록한다.
이 결과는 아래 STEP 3 재고 기능의 구현·검증 완료를 의미하지 않는다.

## 다음 작업과 UI 제안 (2026-09-14)

### 추가 등록 후속 구현

- 전체 음식 카드는 음식명·개별 구매 건수·위치 태그·수량/단위만 표시한다. 날짜·출처·용량·분류·메모·냉동 정보는 목록에서 제거하고 상세에서 확인한다. 기존 needsAttention 날짜 경고 조건 중 하나 이상이면 해당 구매 항목의 수량/단위 오른쪽에 경고 아이콘 하나를 표시한다. 경고가 없는 항목에는 표시하지 않는다.

- 전체 음식 제목 옆 개수는 1pt 키우고 간격을 넓혔다. 위치 태그·수량·아이콘 사이 가로 간격은 8px이다. 이동 버튼은 흰색 배경과 연한 회색 호버, 다른 음식에 합치기는 연한 보라색 배경과 보라색 호버를 사용한다.

- 날짜 순서는 유통기한·소비기한 / 구매일·개봉일로 변경했다. 라벨과 값의 간격은 기본 10px, 359px 이하에서는 8px이며 좁은 카드 여백도 조정했다. 360·320px에서 줄바꿈과 열 넘침이 없음을 확인했다.

- 구매 카드의 위치 태그와 수량을 같은 상단 행에 배치했다. 숫자는 단위와 같은 15px로 줄이되 굵게 표시한다. 구매 항목 추가는 primary 버튼, 다른 음식에 합치기는 연한 보조 버튼으로 구분한다.

- 날짜 카드 검수: 448·390·360·320px에서 네 항목 전체 날짜(2026.09.14)와 전체 미입력을 비교했다. 320px에서 날짜가 옆 열에 붙는 현상을 확인해 359px 이하에서는 카드 여백·열 간격과 글자 크기를 조정했다. 보완 후 320px에서도 줄바꿈·열 넘침이 없음을 확인했다. 임시 검수 화면은 제거했다.

- 개별 구매 카드의 날짜는 값이 없어도 네 항목을 모두 표시한다(미입력 표기). 소비기한·유통기한 / 구매일·개봉일의 두 열로 배치하고 각 라벨과 날짜값은 같은 줄에 표시한다. 열 안에서 라벨 너비를 고정해 날짜값의 시작 위치를 맞춘다.

- 검색 대기 안내 '음식 이름을 입력하고 검색해 줘.'는 제거했다. 검색 결과 건수·결과 없음만 표시한다.
- `ui-dev` 프로필과 `compose.ui-dev.yml`로 화면 리소스 원본 읽기를 도입했다. local 프로필에 포함되며 Docker에서는 prod,ui-dev와 읽기 전용 UI 마운트로 사용한다. 설정 적용 이후 UI 변경은 재빌드 없이 새로고침으로 확인한다. 자동 브라우저 새로고침과 Java 자동 재빌드는 제공하지 않는다.
- 8082 실행 중 HTML·CSS·JS 원본에 임시 주석을 추가하여 HTTP 응답에 재빌드 없이 반영되는 것을 확인했다. 정적 파일 Cache-Control은 no-store이며 임시 주석은 모두 제거했다.

- 기존 음식 추가에서는 중복되는 음식명 입력란을 숨긴다. 분류는 음식 선택 후 값이 있을 때만 비활성·읽기 전용으로 표시한다. 새로운 음식 등록에서는 음식명·분류 입력란을 표시하고 활성화한다.

- 기존 음식 검색은 최초 전체 목록을 노출하지 않는다. 이름을 입력한 뒤 Enter 또는 '검색' 버튼을 눌러야 일치하는 결과를 표시한다. 빈 검색과 입력 중에는 결과를 숨기고, 검색 결과 없음은 별도 안내한다. 검색 Enter는 등록 폼 제출을 막고 한글 조합 중 Enter와 구분한다.

- 등록 방식 명칭은 새로운 음식으로 등록 / 기존 음식에 추가다. 기존 음식에서는 음식명 입력란을 숨기고 분류는 값이 있을 때만 disabled·readonly로 표시한다. 서버는 이름·분류를 MASTER 기준으로 확정한다.
- 로컬 포트 분리: 이 PC의 Git 제외 `.env`에 `FRIZER_APP_PORT=8082`를 설정했다. Docker 확인 앱은 8082, 사용자 IntelliJ/bootRun은 기본 8080을 사용한다. Compose 기본값과 다른 개발자의 설정은 변경하지 않는다.

- '이건 뭐야?'에서 새로운 음식으로 등록 / 기존 음식에 추가를 선택한다. 기존 음식은 이름으로 검색하며 같은 이름의 음식도 분류·항목 수·식별 번호로 구분해 명시적으로 선택한다.
- 개별 구매 목록의 '구매 항목 추가'는 대상 음식을 미리 선택한 등록 폼으로 연결한다. 추가 완료와 취소는 해당 개별 구매 목록으로 이동한다.
- 기존 음식의 이름·분류는 서버에서 확정하며 조작된 입력으로 변경하지 않는다. 단위는 기존 기본값을 제안하되 구매 항목별로 수정 가능하다. 수량·날짜·출처·보관 위치·메모는 각 항목에 저장한다.
- 선택 UI는 기존 primary(#7057c8)를 연하게 사용한다. 보관 위치는 버튼 방식이며 유통기한·소비기한 / 구매일·개봉일 / 분류 / 메모를 접힘 없이 표시한다. 필수·선택 표기를 유지했다.
- MASTER 잠금과 버전 검증으로 병합·수정과의 충돌 및 같은 추가 요청 재제출을 차단한다. 항목·등록 이력·MASTER 버전 변경은 하나의 트랜잭션으로 처리한다. 기존 항목과 이력은 변경하지 않는다.
- V8에서 출처 미선택은 NULL로 보존하고 명시적 ETC와 구분한다. 상세에는 '선택 안 함', 목록에서는 출처 표시 생략으로 처리한다. 기존 ETC와 과거 이력은 추정 변환하지 않는다.
- 개별 구매 카드에서 반복되던 '보관 중' 상태 문구를 제거했다.
- 최종 검증: 전체 Java 테스트 192개와 bootJar 통과. 이미지 선택기 및 합치기 JavaScript 회귀 검사 통과. 기존 UI를 기대하던 검사는 현재 요약 카드·경고 아이콘·수량 마크업에 맞춰 갱신했다. 실제 저장 검증은 격리된 Testcontainers DB에서 수행했다.
- Docker 앱 재빌드·V8 적용·healthy 확인 완료. 브라우저에서 새 등록/기존 음식 선택, 연한 primary 배경, 두 열 날짜 배치, 분류 읽기 전용, 개별 구매 목록에서의 미리 선택된 추가 진입을 확인했다. 등록 화면 CSS에는 캐시 갱신용 버전 표기를 추가했다.

### 이전 논의 기록 (아래 추가 등록 시안과 출처 결함은 위 후속 구현에 반영)

- 우선 수정할 결함: 등록·수정에서 출처 '선택 안 함'을 ETC(기타)로 변환하고 있다. 미선택과 명시적 기타를 구분해 저장·표시하도록 수정해야 한다. 현재 DB도 source_type NOT NULL DEFAULT 'ETC'이므로 스키마·이력·화면을 함께 변경해야 한다. 기존 ETC는 실제 기타와 미선택을 구분할 근거가 없어 일괄 변환하지 않는다. 아직 수정 전이다.
- 다음 구현 대상: 기존 음식에 개별 구매 항목 추가. 이름이 같다는 이유로 자동 연결하지 않고 사용자가 기존 음식을 선택한다. 아직 미구현이다.
- UI 제안(사용자 검토 전): '이건 뭐야?' 등록 폼 상단에서 '새 음식 등록 / 기존 음식에 추가'를 선택한다. 기존 음식 추가에서는 이름 검색과 후보 선택을 제공하고, 선택한 음식의 이름·분류를 요약해 보여준다. 수량·날짜·출처·보관 위치는 이번 항목 기준으로 입력한다. 제출 버튼은 '구매 항목 추가'로 표시한다.
- UI 제안(사용자 검토 전): 개별 구매 목록의 '구매 항목 추가' 버튼은 해당 음식을 미리 선택한 같은 등록 폼으로 연결한다. 추가 완료 시 해당 개별 구매 목록으로 이동한다.
- 등록 시안 후속 요청: '어디에 보관할까?'는 기존처럼 버튼 형태로 보관 위치를 선택한다. 아래 입력은 유통기한·소비기한 / 구매일·개봉일 / 분류 / 메모 순서이며 필수·선택을 표시한다. 시안에 반영했으며 실제 등록 화면에는 아직 적용하지 않았다. 기존 음식 추가 시 분류는 공유 정보이므로 시안에서는 기존 값을 읽기 전용으로 표시한다.
- 후속 작업: 선택한 구매 항목을 다른 기존 음식으로 이동하거나 새 음식으로 분리한다. 구매 항목과 이력은 유지한다. 전체 병합 취소와는 별개이며, 아래 수량 일부를 나누는 '부분 분리'와도 다른 기능이다. 아직 미구현이다.
- 마지막 항목 이동 후 빈 음식 그룹 처리 등 세부 정책은 구현 전에 확정한다. 소비·폐기는 등록·소속 이동 흐름 다음 순서로 제안한다.

## 설계 및 확정 정책

사용자 확정 정책을 바탕으로 현재 V1~V6 스키마를 대조했다.
아래는 STEP 3 전체 설계다. 1차 구현에 적용된 범위는 위 구현 결과를 기준으로 확인한다.

사용자 후속 지침: 현재 데이터는 테스트용이므로 완벽한 과거 호환에 과도하게 투자하지 않는다.
새 구조의 명확성을 우선하고, 구버전 동시 운영·임시 호환 컬럼·복잡한 운영 복구 계획은 첫 구현에서 제외한다.
테스트 데이터 초기화는 가능한 전환 선택지이며 이 문서 작성 시 실제 데이터 삭제는 수행하지 않는다.

### 1. 사용자 확정 정책

1. quantity_amount는 현재 잔량이다.
2. 최초 수량 컬럼을 별도로 두지 않는다. 신규 유입 이력에 최초 숫자·단위를 보존한다.
3. 기존 비구조화 재고는 수량 작업 전에 사용자 확인으로 구조화한다. 자동 파싱하지 않는다.
4. 단위는 trim하고 합의한 표준 단위만 대표 표기로 정규화한다. 자동 환산하지 않는다.
5. ITEM 상태는 ACTIVE / DEPLETED다.
6. 부분 분리는 기존 ITEM 차감 + 새 ITEM 하나 생성이다. 전량 변경은 분리하지 않는다.
7. stock_batch_id, parent_item_id, operation_id를 도입한다.
8. 기존 changes_text는 유지하고 신규 구조화 수량·JSONB snapshot을 병행한다.
9. 소비·폐기는 한 번에 ITEM 하나를 대상으로 한다.
10. 마지막 유효 소비·폐기만 제한적으로 취소한다.
11. 물리 삭제는 웹의 명시적 오등록 정정 기능이다. ITEM과 관련 History를 함께 삭제한다.
12. 엑셀 첫 출시는 새 음식 등록·기존 음식 추가 구매만 지원한다.

엑셀은 행별 오류 표시·미리보기·반영 직전 재검증·중복 업로드 방지를 제공한다.
한 행이라도 오류가 있으면 전체 미반영하며, 반영 중 실패해도 전체 트랜잭션을 취소한다.
동일 MASTER에 여러 구매분을 추가하는 행은 허용한다. 반복 MASTER ID와 중복 업로드 행은 구분한다.

음식명 중복은 허용한다. MASTER 자동 통합은 하지 않는다. 사용자가 통합해도 ITEM·구매분·이력은 보존한다.

#### 목록·상세 UI 확정 정책 (2026-09-14)

- 목록은 음식(MASTER)별로 묶어 표시한다.
- 음식 카드를 선택하면 상세에서 구매분과 개봉·보관 상태별 재고(ITEM)를 구분해 보여준다.
- 소비·폐기 등 재고 작업은 상세에서 대상 ITEM을 선택해 처리한다.
- 예: 두부 상세에 서로 다른 구매분의 미개봉 1모와 개봉 0.5모를 구분해 표시한다.
- 이 결정은 화면 구조에 대한 확정이며 STEP 3 구현 완료를 의미하지 않는다.
- 보관 위치 필터에서의 표시 범위, 서로 다른 단위의 수량 요약 등 세부 표시는 후속 논의한다.

#### 음식 병합 확정 정책 (2026-09-14)

- 사용자가 원본과 기준 음식을 선택한다. 이름을 먼저 수정할 필요가 없다.
- 예: `듀부`를 `두부`에 합치면 모든 구매분이 `두부`에 속한다. 구매분 자체는 합치거나 환산하지 않는다.
- 기준 음식의 이름과 기본 설정을 유지하며 원본 음식 항목은 제거한다.
- 연결된 재고와 이력은 보존한다. 과거에 기록된 음식명은 병합으로 덮어쓰지 않는다.
- 실행 전 이동할 재고와 보존할 이력 건수를 표시하고 확인받는다.
- 이번 병합에는 되돌리기 기능을 제공하지 않으며 확인 화면에 안내한다.

### 2. V6 기준 구현과 전체 설계의 차이

- food_item.food_id와 food_history.food_id가 현재 재고·이력 연결 키다. 이 ID를 보존한다.
- 현재 food_item에 음식명·분류와 실제 재고 정보가 함께 있다.
- quantity_amount는 NUMERIC(12,3), quantity_unit은 VARCHAR(10), quantity_text는 VARCHAR(50)이다.
- 신규 입력은 정수 9자리·소수 2자리이며 DB 수량 제약은 현재 0 초과다.
- 기존 비구조화 재고는 amount/unit 모두 NULL이며 quantity_text를 보존한다.
- 현재 상태는 ACTIVE / CONSUMED / DISCARDED다. 소비·폐기 기능 자체는 아직 미구현이다.
- 등록 이력은 changes_text TEXT에 snapshot-v1: 접두어와 JSON을 저장한다.
- 수정 이력은 changes_text에 변경 항목과 전후 값을 문자열로 저장한다.
- 현재 수정 충돌은 행 잠금과 expectedUpdatedAt 비교로 방지한다.
- MASTER, 구매분 식별, 분리, 취소, JSONB 컬럼은 아직 없다.

### 3. food_master 신규 테이블

| 컬럼 | 타입 / NULL | 의미 |
|---|---|---|
| master_id | BIGINT identity PK | 음식 식별자 |
| food_name | VARCHAR(100) NOT NULL | 대표 음식명, 공백만 입력 금지, UNIQUE 없음 |
| category | VARCHAR(50) NULL | 음식 분류 |
| default_quantity_unit | VARCHAR(10) NULL | 신규 ITEM 입력 기본값. 기존 ITEM 단위 변경 없음 |
| default_exclude_expiry_warning | BOOLEAN NOT NULL DEFAULT FALSE | 신규 ITEM 기한 경고 제외 기본값 |
| default_exclude_opened_warning | BOOLEAN NOT NULL DEFAULT FALSE | 신규 ITEM 개봉 경고 제외 기본값 |
| version_no | BIGINT NOT NULL DEFAULT 0 | 수정·통합·삭제 및 업로드 확인용 버전 |
| created_at | TIMESTAMPTZ NOT NULL | 생성 시각 |
| updated_at | TIMESTAMPTZ NOT NULL | 변경 시각 |

재고가 없어도 MASTER를 자동 삭제하지 않는다. 목록 노출과 추가 등록 검색은 별도로 처리한다.
MASTER 기본 설정은 신규 ITEM에 복사한다. 기존 ITEM 일괄 변경은 별도 명시적 작업이다.
대표 단위의 정확한 매핑표는 구현 전에 정하며, 자유 입력 영문 전체를 소문자로 바꾸지 않는다.

### 4. food_item 유지·확장

물리 PK 이름은 food_id를 유지한다. 설계에서 ITEM ID라 부르는 값이 이 컬럼이다.

| 컬럼 | 타입 / NULL | 의미 |
|---|---|---|
| food_id | 기존 BIGINT PK 유지 | ITEM 식별자, 기존 URL·이력 연결 보존 |
| master_id | BIGINT NOT NULL, FK RESTRICT | 소속 음식 |
| stock_batch_id | UUID NOT NULL | 원래 구매·등록분. 신규 유입마다 생성, 분리 시 공유 |
| parent_item_id | BIGINT NULL, 자기 FK RESTRICT | 직접 분리된 원본 ITEM |
| quantity_amount | 기존 NUMERIC(12,3) NULL | 현재 잔량. 타입 축소 없음 |
| quantity_unit | 기존 VARCHAR(10) NULL | 실제 재고 단위 |
| quantity_text | 기존 VARCHAR(50) NULL | 구조화 값의 표시 문자열 또는 기존 원문 |
| status | VARCHAR(20) NOT NULL | ACTIVE / DEPLETED |
| exclude_expiry_warning | BOOLEAN NOT NULL DEFAULT FALSE | 해당 ITEM 기한 경고 제외 |
| exclude_opened_warning | BOOLEAN NOT NULL DEFAULT FALSE | 해당 ITEM 개봉 경고 제외 |
| version_no | BIGINT NOT NULL DEFAULT 0 | 상태 변경 충돌 검증 |

나머지 기존 컬럼을 유지한다: storage_type, capacity_text, expired_at, sell_by_at,
purchased_at, opened_at, frozen_at, source_type, source_memo, freeze_type, memo,
created_at, updated_at. 기존 날짜·출처·위치 제약은 새 모델과 함께 검증한다.

food_name/category는 MASTER로 읽기·쓰기 책임을 옮긴다. 새 앱과 함께 전환하며
필요한 값의 복사 후 ITEM의 중복 컬럼은 제거한다. 구버전 코드 호환용 이중 쓰기는 하지 않는다.

수량·상태 제약:
- 비구조화 예외: amount/unit 모두 NULL이고 ACTIVE. 기존 quantity_text를 그대로 표시한다.
- 구조화 ACTIVE: amount > 0, 유효한 unit 존재.
- 구조화 DEPLETED: amount = 0, 유효한 unit 존재.
- 단위는 공백만 허용하지 않고 최대 10자. 기존 숫자 상한 유지.
- 새 등록·추가 구매는 0 초과. 소비·폐기 처리량도 0 초과.
- amount/unit 중 하나만 NULL인 상태는 금지한다.
- 구조화 값 변경 시 Service가 quantity_text를 재생성한다.

분리 제약:
- 0 < 분리량 < 현재 잔량. 전량 개봉·이동은 동일 ITEM 수정.
- 부모와 자식은 같은 master_id, stock_batch_id, 단위를 유지한다.
- 분리량만큼 기존 ITEM을 줄이고 새 ITEM을 하나 만든다. 같은 트랜잭션으로 처리한다.
- 자식은 기존 구매 정보와 날짜를 복사한 뒤 요청한 관리 조건만 변경한다.
- 신규 자식만 부모를 지정하고, 재부모 지정은 하지 않는다. 자신을 부모로 지정하지 못한다.
- stock_batch_id 자체는 유입 수량을 나타내지 않는다. 최초 수량은 유입 이력이 근거다.

인덱스: master_id, stock_batch_id, parent_item_id 및 현재 ACTIVE 날짜 검색 인덱스를 유지·검토한다.

### 5. food_history 유지·확장

기존 PK와 food_id FK, action_type, previous_storage_type, new_storage_type,
quantity_text, memo, created_at, changes_text를 보존한다. 기존 행의 원문은 재작성하지 않는다.

| 추가 컬럼 | 타입 / NULL | 의미 |
|---|---|---|
| operation_id | UUID NULL | 신규 작업에서 필수. 같은 작업의 여러 History를 연결 |
| processed_quantity_amount | NUMERIC(12,3) NULL | 해당 ITEM에 적용한 양 |
| quantity_unit | VARCHAR(10) NULL | 당시 단위 |
| before_quantity_amount | NUMERIC(12,3) NULL | 작업 전 해당 ITEM 잔량 |
| after_quantity_amount | NUMERIC(12,3) NULL | 작업 후 해당 ITEM 잔량 |
| snapshot_version | INTEGER NULL | 신규 스냅샷 형식 버전 |
| before_snapshot | JSONB NULL | 작업 전 구조화 원본 값 |
| after_snapshot | JSONB NULL | 작업 후 구조화 원본 값 |
| reversal_of_history_id | BIGINT NULL, 자기 FK RESTRICT, UNIQUE | 취소 대상 |
| before_item_version | BIGINT NULL | 작업 전 ITEM 버전 |
| after_item_version | BIGINT NULL | 작업 후 ITEM 버전 |

기존 행은 신규 컬럼 NULL을 허용한다. operation_id가 있는 신규 행에는 버전 및
작업별 필요한 수량·스냅샷 필드의 정합성을 강제한다. 허용할 action_type과 기존
위치 전이 CHECK를 함께 갱신한다. CREATE/UPDATE 분기만 추가해서 끝내지 않는다.

신규 작업 종류:
CREATE, ADD_STOCK, UPDATE, STRUCTURE_QUANTITY, CONSUME, DISCARD,
OPEN, FREEZE, MOVE, SPLIT, CANCEL.

- STRUCTURE_QUANTITY는 과거 자유 입력을 현재 잔량으로 확인하는 작업이다. 구매량 증가 없음.
- CREATE/ADD_STOCK은 실제 신규 유입에만 사용한다. 분리 자식에 CREATE를 남기지 않는다.
- 부분 개봉·이동은 같은 operation_id의 SPLIT 이력 2건으로 기록한다.
  부모 이력은 n → n-q, 자식 이력은 생성 전 NULL → q이다.
  스냅샷에 분리 목적(OPEN/MOVE 등), 원본·신규 ITEM ID, 구매분 ID를 보존한다.
- SPLIT의 두 이력에 같은 양이 있어도 구매·소비·폐기량에 더하지 않는다.
- 전량 개봉·이동은 OPEN/MOVE/FREEZE 이력 1건이다.
- CANCEL은 원본 이력을 삭제하지 않고 reversal_of_history_id로 연결한다.
- JSONB에는 표시 문장 대신 안정된 필드명, 숫자, 날짜, enum 코드와 명시적 null을 저장한다.
- 당시 음식명과 master_id, 구매분 연결은 스냅샷에도 보존한다.
- 현재 소속 MASTER는 ITEM에서 조회한다. History에 현재 master_id를 중복 FK로 두지 않는다.
- 과거 등록 snapshot-v1과 수정 changes_text 표시를 계속 지원한다.

집계:
- 신규 유입량은 CREATE/ADD_STOCK만 포함한다. SPLIT/OPEN/MOVE/UPDATE/CANCEL은 유입이 아니다.
- 출처에 직접 조리·배달 잔반도 있으므로 전체는 '총등록량/총유입량'이다.
  실제 '총구매량'은 이 중 당시 source_type=PURCHASE인 기록만 집계한다.
- 소비·폐기는 원본 처리량에서 유효한 취소를 제외해 계산한다.
- 숫자가 없는 과거 이력은 구매량 0으로 간주하거나 현재 잔량으로 역산하지 않는다.
- 물리 삭제된 기록은 집계에서도 사라진다. 삭제는 통계 영구 보존과 양립하지 않는 명시적 기능이다.

### 6. 취소와 충돌

- 같은 ITEM의 마지막 유효 CONSUME/DISCARD만 후보이며 이미 취소된 이력은 제외한다.
- 이후 소비·폐기·수량 정정·단위 변경·구조화·이동·개봉·분리가 있으면 차단한다.
- 소비 후 소비 후 취소처럼 연결된 후속 수량 작업이 있는 과거 처리는 첫 버전에서 취소하지 않는다.
- 음식명·메모·경고 설정 등 수량과 충돌하지 않는 변경은 유지한다.
- 날짜 변경도 실제 개봉·이동 등의 상태 변경에 해당하는지 구분한다.
- 취소 시 현재 잔량에 원래 처리량을 더하고 quantity_text/status만 재계산한다.
- snapshot 전체 복원 금지. 날짜·위치·개봉·음식명·메모·경고 설정을 덮어쓰지 않는다.
- 현재 화면 버전 검사와 취소 가능성 검사는 별개다. 버전 불일치는 새로고침 후 재검증한다.
- ITEM 갱신, 취소 이력 INSERT, 중복 취소 방지는 한 트랜잭션이다.

version_no는 모든 ITEM 변경 시 증가시킨다. 수량 이상 변경은 action_type만 보지 않고
전후 구조화 필드로 확인한다. 기존 updatedAt 비교를 버전 기반으로 전환하되 어느 경로도
충돌 검증을 빠뜨리지 않는다.

operation_id는 이력 연결 키이지 그것만으로 중복 제출 방지를 보장하지 않는다.
신규 쓰기 요청에 재사용 가능한 요청 토큰과 요청 내용 식별값을 적용하고, DB UNIQUE로
완료 결과를 한 번만 확정하는 별도 operation/request 기록을 둔다. 같은 토큰·다른 내용은 거부한다.
엑셀은 같은 업로드의 확인 재시도뿐 아니라 파일 재업로드의 신규 행 중복도 별도 식별한다.
이 기록의 세부 컬럼과 업로드 행 식별 규칙은 업로드 구현 설계에서 구체화한다.

### 7. 물리 삭제

- 웹의 오등록 정정 도구로만 제공한다. 소비·폐기와 별도 기능이다.
- 대상 ITEM 수·History 수와 다른 재고에 미치는 연결 영향을 먼저 보여주고 재확인한다.
- 실제 실행 시 대상 집합·버전·건수를 다시 검증한다. 확인 뒤 변경되면 재확인한다.
- MASTER 잠금을 기준으로 추가 등록·분리·통합·삭제와의 경쟁을 직렬화한다.
- History 삭제 → ITEM 삭제 → 요청한 경우 MASTER 삭제 순서로 같은 트랜잭션에서 처리한다.
- MASTER/ITEM/History FK는 RESTRICT 유지. 광범위한 CASCADE를 사용하지 않는다.
- 취소 History를 원본 History보다 먼저 지우는 등 자기 FK 의존성을 처리한다.
- ITEM만 삭제할 때 비어 있는 MASTER를 자동으로 삭제하지 않는다.

분리 관계 처리 제안:
- 부모 ITEM만 삭제해도 살아 있는 자식 ITEM을 자동 삭제하지 않는다.
- 삭제 전 미리보기에 영향을 받는 자식 연결 건수를 표시한다.
- 생존 자식의 parent_item_id를 명시적으로 NULL 처리하고 버전을 갱신한 후 부모를 삭제한다.
- 생존 자식의 stock_batch_id와 당시 분리 snapshot은 유지한다.
- 같은 operation_id라는 이유로 생존 ITEM의 이력을 함께 삭제하지 않는다.
- 부모 등록 이력이 삭제되면 구매분 최초 수량이 확인 불가능할 수 있음을 인정한다.
  생존 자식 수량을 신규 구매량으로 대체하지 않는다.

위 생존 자식 처리 방식은 확정된 삭제 범위를 구현하기 위한 제안이며 실제 UI에 드러내야 한다.

### 8. Flyway 전환 전략 — 테스트 단계에 맞춰 단순화

원칙: 이미 적용된 V1~V6은 수정하지 않는다. 실제 최신 migration 번호를 구현 시 다시 확인한다.

전체 전환은 기능 단계별 migration과 새 앱을 함께 적용한다. V7은 음식 그룹·병합 기반을 담당하고,
수량·분리·취소 확장은 후속 migration으로 이어간다. 각 단계에서 구버전 앱과 동시 운영하지 않는다.
- MASTER 생성, ITEM 확장, History 구조화 컬럼, 상태·수량 제약과 인덱스를 함께 적용한다.
- 기존 앱은 전환 중 중지한다. 구버전 앱과의 동시 운영은 지원하지 않는다.
- 간단하게 보존할 수 있는 기존 재고는 ITEM 1건당 MASTER 1개로 연결한다. 이름 기준 통합 금지.
- 기존 ID와 changes_text는 유지할 수 있으면 유지한다. 자유 문자열이나 과거 이력의 정교한 역산은 하지 않는다.
- food_name/category를 MASTER로 복사한 후 ITEM 중복 컬럼을 제거한다.
- ACTIVE 비구조화 재고를 유지하는 경우 기존 원문·NULL 쌍과 수동 구조화 정책을 적용한다.
- 과거 소진 상태·모순된 테스트 데이터 때문에 복잡한 변환이 필요하면 별도 호환 계층을 만들지 않는다.
  실제 대상 데이터를 확인하고 테스트 데이터 초기화 경로를 선택할 수 있다.
- 데이터 초기화는 별도 명시적 실행으로 구분한다. 앱 시작 때 자동으로 데이터를 지우지 않는다.
- 새 빈 테스트 DB에서 V1~새 migration 전체 실행과 새 시나리오 fixture를 검증한다.
- 보존 전환을 선택한 경우 해당 기존 데이터 fixture에서도 migration을 검증한다.
- 운영용 단계적 배포·복잡한 rollback 자동화는 실사용 배포 단계에서 필요에 맞춰 마련한다.

### 9. 구현 완료 검증 기준

- 보존 전환 선택 시 기존 ITEM/History ID·건수·원문 보존, MASTER 매핑 누락 없음, 자동 이름 통합 없음.
- 신규 입력/기존 비구조화 조회와 구조화, 0 소진, 수량 초과·중복 처리 방지.
- 두부 2모 → 1모 개봉 → 0.5모 소비 → 나머지 폐기 → 취소.
- 기존 구매분 1모 + 신규 구매분 2모에서 오래된 1모만 폐기.
- 분리 후 구매량 증가 없음, 분리 전후 총수량 보존, 다단계 부모 연결.
- 메모 변경 보존 취소, 개봉/이동/분리 후 취소 차단, 이중 취소 방지.
- 삭제의 자기 FK·생존 자식·취소 이력 처리, 확인 이후 변경 시 재확인, 실패 시 전체 복원.
- 과거 snapshot-v1/changes_text와 신규 JSONB의 이력 화면 호환.
- 엑셀 오류 한 건 시 전체 미반영, 미리보기 뒤 MASTER 변경·삭제 재검증, 중복 업로드 방지.
- 기존 날짜 정책·쪼코 이미지 선택 정책 유지. 테스트 데이터 보존 여부는 선택한 전환 경로에 따른다.

목록·상세의 수량 요약과 후속 액션 UI, 대표 단위 매핑, 업로드 식별 방식은 이 정책 위에서 구체화한다.

## 자동 검증

**1차 범위 자동 검증 완료.** 후속 수량 작업과 엑셀 검증은 포함하지 않는다.

| 검증 | 결과 |
| --- | --- |
| V7 migration 적용·기존 데이터 보존·이름 중복 미통합 | 통과 |
| 병합·기준 설정 유지·이력 보존·재제출·동시 요청·실패 롤백·수정 충돌 | 통과 |
| 수량·분리·소비·폐기·취소·삭제 통합 테스트 | 미실행 |
| 엑셀 등록·추가 구매 검증 | 미실행 |
| 전체 Java 테스트 및 JavaScript 회귀 검증 | 2026-09-14 히스토리 보완 후 Java 378개, 실패·오류·건너뜀 0 / 등록 UI 25개·이미지 선택기·합치기 JS 검사 통과. [목록](STEP3_TEST_MATRIX.md) |
| bootJar 및 Docker 이미지 빌드 | 통과 |

검증 환경: Java 21, Docker PostgreSQL 17.11. `gradlew.bat --no-daemon test bootJar`,
`node src/test/js/choco-selector.test.cjs`를 실행했다. 이후 화면의 KST 표시·카드 배치 수정 후
`test --tests '*FoodMergeIntegrationTest' bootJar`로 관련 9개 테스트와 이미지 선택기를 다시 검증했다.
테스트 보고서 `build/reports/tests/test/index.html`은 가장 최근 실행 범위를 반영한다.
후속 카드 UI·버튼 활성 조건 수정 후 관련 테스트 10개와 이미지 선택기가 통과했다.
390×844 화면에서 외부 패널 제거·날짜 정렬·단일 음식 병합 버튼 비활성화를 재확인했다.
동시 재제출에서 발견한 MyBatis 캐시 문제는 MASTER 잠금 조회의 캐시 무효화로 해결했다.

## 브라우저 확인

**1차 범위 브라우저 확인 완료.** 별도 검증용 DB와 8082 앱에서 `두부`, `듀부` 등록 →
원본 상세 → 병합 대상 선택 → 재고 1건·이력 1건 확인 → 병합 → 두부 아래 구매분 2건 유지까지 확인했다.
390×844 모바일에서 음식별 목록과 구매분 상세 배치, 한국 시간 표시를 확인했다.
초기 검증용 앱과 DB는 종료·정리했다. 현재 확인용 Docker 앱은 8082에서 실행하며 사용자 8080은 별도로 유지한다.
추가 구매 UI는 후속 브라우저 확인을 완료했다. 부분 개봉·소비·폐기·취소·오등록 삭제·엑셀은 아직 구현·브라우저 확인 대상이 아니다.

## 남은 범위

- 실기기 Safari/Android·모바일 IME·부하·장애 주입의 미검증 범위는 [테스트 보고서](STEP3_TEST_MATRIX.md)의 남은 범위를 따른다. V9와 Java 변경을 사용자 실행 앱에 적용하려면 재시작이 필요하다. 이번에는 테스트 DB에만 V9를 적용했다.
- 컬럼·제약·생존 자식 삭제 연결 처리 등 구현 제안 구체화.
- 확정된 음식별 목록·ITEM별 상세 구조에 따른 세부 UI와 재고 액션, 대표 단위 매핑 결정.
- 후속 migration과 ITEM 잔량·분리·소진 상태·구조화 이력·취소·오등록 삭제 구현.
- 엑셀 등록·추가 구매와 요청·행 중복 방지 구현.
- 후속 기능별 자동 검증과 브라우저 확인 후 위 결과 갱신.

엑셀 수정·소비·폐기는 후속 범위다. 통계·추천 점수·PWA·인증·배포는 이번 범위에 자동 포함하지 않는다.

문서 규격: 앞으로 STEP 보고서는 작성일·범위·상태와 함께 `구현 결과`,
필요시 `설계 및 확정 정책`, `자동 검증`, `브라우저 확인`, `남은 범위` 순서를 유지한다.
설계 중·미구현·미검증 항목을 완료 결과와 섞지 않고, 구현 완료 후 같은 파일에 실제 결과를 기록한다.
