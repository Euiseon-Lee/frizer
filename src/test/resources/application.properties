# Existing integration suites use isolated databases and test business behavior.
# SecurityIntegrationTest explicitly overrides this and exercises the real filter chain.
frizer.security.enabled=false
